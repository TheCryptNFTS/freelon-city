# FREELON CITY — Upgrade Plan (research + roadmap)

_Generated 2026-06-19 from a 19-agent sweep: exhaustive code audit (every TODO / half-built / disabled feature), live competitive web research (beg/borrow/steal from Pudgy, Azuki, Milady, Remilio, Virtuals, ElizaOS, Parallel, Freysa, Marvel Snap, Balatro, Slay the Spire, Galxe, Layer3, friend.tech, Duolingo, Wordle, Spotify, Privy…), and 6 specialist roles. **249 raw ideas → 249 deduped.** Legend: [impact·effort] · 📁 from our code · 💡 stolen · ✦ original._

> ## 🚨 MUST-FIX FIRST — a floor-coercion residual is STILL LIVE
> The dump-deterrent rip-out missed one path: the **Red Signal snipe bounty still fires in `lib/holder-tick.ts:178`** (and `lib/economy-extras.ts` Listing-bounty / Sale-share are floor/market-coupled HEX faucets). These violate the no-floor-coercion rule we just enforced AND burn OpenSea API quota on a dead feature. **Retire them before anything else.**

## 🔌 TURN ON THE MONEY — built but switched OFF (highest ROI in the whole list)
_The single biggest finding: a stack of finished features are disabled. The HEX **sink doesn't even work** (every mission costs 0⬡), the ETH pay rails are off, and several complete pipelines are one flag away. Flipping these is the cheapest, highest-return work on the board._

- **[5·S]** Flip all 8 mission costs from 0 to real HEX prices — the sink has been free since launch — _📁 lib/missions/catalog.ts:36_
  - Every registered mission in lib/missions/catalog.ts has cost: 0 with a comment 'FREE during internal testing' (lines 36, 54, 71, 91, 109, 129). The PREMIUM_HEX table in economy-constants.ts (lines 269-279) already has correct prices: strategy 1500, risk 1500, dossier 2500, crew 2000, deploy-crew 1200, deploy-citizen 800, deploy-video 4000. The mission endpoint at app/api/citizens/[id]/mission/route.ts already reads premiumHexFor(mission.id) and charges it — but catalog.ts sets cost:0 which short-circuits the charge gate. Wire catalog.ts cost fields to premiumHexFor() values for the non-free abilities. Remove '(Free internal test.)' from every description string. This activates the only existing HEX sink beyond Guard the Pot and finally validates whether holders will pay HEX for agent work. Keep deploy-video at 0 until REPLICATE_API_TOKEN is configured (it already 503s safely).
- **[5·S]** Wire EVOLVE_LIVE=true after setBaseURI — the art-evolution pipeline is fully built — _📁 app/api/agent/evolve/route.ts:50_
  - The art evolution feature (HEX sink: burn ⬡ to AI-render an upgraded version of your citizen) is 100% built at app/api/agent/evolve/route.ts. The gate is a single env-var check at line 50: 'if (process.env.EVOLVE_LIVE !== true) return 410 coming_soon'. Auth, ownership check, tier/level gating, render pipeline (generateEvolvedArt), refund-on-failure, and memory logging are all present. The only prerequisite is the on-chain metadata migration (setBaseURI) so the evolved image can be served. Once that is done, set EVOLVE_LIVE=true in Vercel. This is the highest-HEX sink in the catalog (evolveCost per tier) and the most defensible holder-only power. The status endpoint at app/api/agent/evolve/status/route.ts will flip automatically with the same flag.
- **[5·S]** Kill the still-live Red Signal snipe bounty — it violates the no-floor-coercion hard rule and is a floor-tied HEX faucet — _📁 phase3/freelon-city-site/lib/holder-tick.ts:178_
  - The dump-deterrent RIP-OUT only removed the PENALTY half. creditSnipeBounties in lib/economy-extras.ts is STILL called live by runHolderTick (holder-tick.ts:178, reached via app/api/wallet/[address]/hex/route.ts:56). It pays real HEX = (floorEth - priceEth) × HEX_PER_ETH (capped 500) for BUYING any listing priced ≤90% of floor (RED_SIGNAL_THRESHOLD=0.9). That is exactly a reward tied to selling/listing below a floor — the forbidden mechanic, and a HEX faucet whose size is set by the floor (a number we don't control and that can be manipulated by a sybil pair listing low then buying with an alt). The 14-day hold + self-snipe filter reduce but do not remove the wash incentive. FIX: delete the creditSnipeBounties call from holder-tick, retire RED_SIGNAL_*/SNIPE_* constants to 0/removed, and stop writing red-signal-store. This is a go-live blocker, not a feature.
- **[5·M]** Payments: flip PAYMENTS_LIVE and surface the Strategy ($12) + Dossier ($19) ETH pay rail to holders — _📁 lib/missions/pricing.ts:11 (PAYMENTS_LIVE=false) + app/api/citizens/[id]/mission/route.ts:316-335 (ETH debit path exists)_
  - lib/missions/pricing.ts:11 shows PAYMENTS_LIVE = false (default). The entire ETH per-mission payment rail (strategy $12, risk $12, dossier $19, deploy-citizen $5) is built and gated behind this single env flag. The unlock rail is already live (PAYMENTS_LIVE gates the unlock at /api/citizens/[id]/unlock/route.ts). The per-mission ETH payment path in the mission route (lines 316-335) is also complete with verifyMessage + debitWalletHex. The only missing piece is the quote→pay→confirm flow on the client (AgentWorkspace) for per-mission ETH — similar to the unlock WorkspaceUnlock component. Flipping the flag without the client pay flow would break UX, but the backend is ready. M effort = build the client pay modal for per-mission ETH; the server-side is done.
- **[5·M]** Play YOUR FREELONs deck — wire the dead createOwnedNftMatch path — _📁 /Users/billy/crypt-game/src/engine/createOwnedNftMatch.ts:57_
  - The whole ownership-tie-in engine already exists and is UNUSED: createOwnedNftMatch.ts builds a real 30-card deck from a wallet's token ids (tcg_<tokenId>), but getOwnedNftCardIds.ts literally comments 'createOwnedNftMatch sat unused.' Wire a 'Play your collection' mode: connected wallet -> getOwnedNftCardIds -> buildNftDeck -> a real match where the player commands the cards they actually OWN. This is the single highest-leverage holder hook in the codebase — it converts 'a card game exists' into 'MY cards fight.' Gate gracefully (guests still get the curated solo deck). Make the result/share card stamp 'Fought with owned cards.'
- **[5·M]** Make Guard the Pot the flagship public HEX sink + acquisition spectacle — finish and launch it — _📁 phase3/freelon-city-site/lib/economy-constants.ts:358_
  - GUARD_POT (economy-constants.ts:366) is fully built behind GUARD_POT_LIVE: escalating ⬡ fee (BASE 100, ×1.015/attempt, FEE_MAX 10000), 100% burned, per-wallet 20/day + global 2000/day caps bounding LLM cost, prize is EXTERNAL to ⬡ so it can't drain supply. This is the single best net-sink we have AND the most shareable artifact (an AI agent guarding a real prize = Freysa-style virality, which is the known bottleneck). It's been 'flagged-but-not-built' for the launch. Plan: wire a founder-seeded prize (ETH or a 1/1 FREELON, external to ⬡), add an OG share card of the running transcript + pot size, a public leaderboard of best jailbreak attempts, and ship the launch switch. Every attempt deflates HEX and markets the brand.
- **[5·L]** Bridge Crypt win-rewards to city HEX wallet — the TODO stub is the only gap — _📁 crypt-game/src/meta/rewards.ts:489_
  - crypt-game/src/meta/rewards.ts line 489 and src/meta/progression.ts line 204 both have an explicit TODO(owner) comment: 'Any reward that would grant real hex / on-chain assets / wallet writes lives here'. Currently all in-game HEX is device-local (stored in localStorage, labelled '(device)' in the UI at src/pages/HomePage.tsx:315). The city already has a live play-event telemetry endpoint at /api/play-event that receives sendBeacon posts from the game (crypt/src/lib/analytics.ts:151). Extend that endpoint (or add a sister /api/play-reward endpoint) to accept a signed wallet + match-result payload and credit the city HEX wallet. This is the loop: play Crypt → earn city HEX → spend on agent missions. Without it, the two products are isolated silos with no holder incentive to cross between them.
- **[4·S]** Register feud and versus missions in the catalog so holders can trigger them from the workspace composer — _📁 lib/missions/catalog.ts:84_
  - lib/missions/catalog.ts:84 explicitly states 'feud / versus stay UNregistered (no UI, no owned-only gate yet).' Both resolvers are complete (resolvers/feud.ts, resolvers/versus.ts), tested, and owned-only gated. The versus route at /api/citizens/[id]/versus also exists as a standalone endpoint. Registering them in the catalog lets the workspace composer surface them as ability tasks. Feud (two citizens co-produce lore) and Versus (red-team clash) are the two highest-virality outputs in the system — every result advertises two NFTs and pulls the other holder into the loop. Both are free tier so no payment complexity.
- **[4·S]** Video generation: wire REPLICATE_API_TOKEN and surface Animate Citizen as a premium tier product — _📁 lib/missions/video-gen.ts:8 + app/api/citizens/[id]/mission/route.ts:211_
  - lib/missions/resolvers/deploy-video.ts and lib/missions/video-gen.ts are fully built. The resolver gracefully returns 'Video is coming soon' when REPLICATE_API_TOKEN is absent. The mission is registered in catalog.ts:47-61 with a skill gate (design minLevel 3). The unlock gate at app/api/citizens/[id]/mission/route.ts:211 already checks for REPLICATE_API_TOKEN before charging HEX. The only missing piece is setting REPLICATE_API_TOKEN in Vercel env and choosing a working Replicate model slug (REPLICATE_VIDEO_MODEL env, default 'minimax/video-01'). No code change required — the entire pipeline runs on env toggle. Premium HEX cost and refund-on-failure are already wired. Animate Citizen is the premium visual product tier above Deploy (static image).
- **[4·S]** Register the 'Feud' mission — the resolver is complete and viral by construction — _📁 lib/missions/resolvers/feud.ts:1_
  - lib/missions/resolvers/feud.ts is a fully-written LLM resolver (two citizens, one output — rivalry exchange / duet / heist) that the catalog comment explicitly deferred 'until they ship with a UI + owned-only gate' (catalog.ts:84). Unlike crew (which is already registered), feud is cross-holder viral: two different holders' citizens collaborate, generating output that advertises both NFTs and pulls a second holder into the loop. The resolver exists, test stubs confirm it is correctly unregistered, and the /api/citizens/[id]/versus route is already live. Register feud in catalog.ts the same way crew was registered (owned-only guard is in the resolver: line 42 refuses self-feud). Add a surface to AgentPowers.tsx alongside the existing Versus tab (which uses the /versus endpoint, not feud). Tag output as 'social' category so it does not pollute the professional résumé. Cost: free or low-HEX to maximise virality.
- **[4·S]** Flip GUARD_POT_LIVE=true and fill the [BRACKETED] legal blanks — the full backend is built — _📁 app/api/play/guard/attempt/route.ts:67_
  - Guard the Pot is 100% code-complete and flag-gated: attempt route at app/api/play/guard/attempt/route.ts line 67 checks GUARD_POT_LIVE, state route at line 23 checks the same flag, the /play page card is gated at app/play/page.tsx:100, and the cron pulse is gated at app/api/cron/crack-pulse/route.ts:41. The only blockers are: (1) legal — app/legal/guard-the-pot-rules/page.tsx:27 has [SPONSOR LEGAL ENTITY], line 81 has [PRIZE DESCRIPTION] and [ARV], and the page footer says 'DRAFT — pending attorney review' at line 114; (2) setting GUARD_POT_LIVE=true in Vercel. The feature is the project's best standalone virality driver (HEX sink + public pot + weekly tweets via tweetGuardPot). Fill the brackets, get counsel sign-off, set the env var.
- **[4·S]** Activate video-generation mission by configuring REPLICATE_API_TOKEN — _📁 lib/missions/resolvers/deploy-video.ts:6_
  - The deploy-video mission resolver at lib/missions/resolvers/deploy-video.ts is complete and ships 'keyless-safe': when REPLICATE_API_TOKEN is unset it returns error:no_video_provider and the endpoint at app/api/citizens/[id]/mission/route.ts:211 returns a clean 503 'Video is coming soon'. The premium HEX cost is already configured (4000⬡ at economy-constants.ts:276). lib/missions/video-gen.ts line 66 shows the single env-var guard. Once REPLICATE_API_TOKEN is set in Vercel, the gate flips and the most expensive HEX sink in the catalog becomes live. The dashboard already shows it conditionally (CitizenAgentDashboard.tsx:62: 'only shown when a provider is configured').
- **[4·S]** Reliquary shop — activate the cosmetic purchase flow with Sigil — _📁 src/pages/ShopPage.tsx:9_
  - ShopPage.tsx is a pure preview shell: 'TODO: payment rails, inventory, pricing, seasonal SKUs; no API calls here.' But purchaseCosmetic() in rewards.ts is fully implemented as a Sigil-only (in-game, zero real value) purchase. COSMETIC_CATALOG has 5 buyable items (frame_signal 200 Sigil, frame_oracle 400, frame_void 600, sleeve_hex 150, sleeve_static 250). The player's Sigil balance is tracked. Wire a live shop grid in ShopPage that calls purchaseCosmetic() locally and shows owned/unowned state. No payment rails needed — Sigil only, fully self-contained.
- **[4·S]** Marketplace — show real owned NFT cards in the grid, not just the full catalog — _📁 src/pages/MarketplacePage.tsx:37-69_
  - MarketplacePage.tsx displays all 4000+ playable cards with mock listings from getMockListings(). But walletOwnership.ts and fetchOwnedCards.ts already fetch the viewer's real owned card ids from wallet/OpenSea. The useWalletConnection hook is already mounted on MarketplacePage with the viewer's address. Add an 'Owned' tab that filters the grid to only cards in the wallet. This turns the marketplace from a generic catalog into a personal collection view, directly relevant to the real holders who are the primary audience.
- **[4·S]** Streak Freeze mechanic — protect a streak with HEX — _💡 Duolingo_
  - Steal Duolingo's streak-freeze: holders can spend HEX (SINK) to buy a 1-day 'Signal Buffer' that protects their daily streak if they miss a day. HEX spend is the sink. Streak protection is the motivation. Creates a direct reason to accumulate HEX rather than ignore it. Price calibrated so a 30-day streak is achievable with normal play but a 60-day streak requires deliberate HEX spend. No floor mechanics, no selling penalties — pure voluntary sink tied to a status number the holder wants to keep. Duolingo data: users 2.3x more likely to engage daily once they hit 7+ days; protecting that streak creates the same pull.
- **[4·S]** Retire Listing bounty + reframe Sale share — both are floor/market-coupled HEX faucets that survived the rip-out — _📁 phase3/freelon-city-site/lib/economy-extras.ts:310_
  - creditListingBounty (economy-extras.ts:310) pays 5⬡/day per active listing (cap 25/day) just for KEEPING art listed — a liquidity-shaping reward of the same family the rip-out condemned, still rendered on /earn (app/earn/page.tsx:175). creditSaleShare (economy-extras.ts:48) pays 5% of sale ETH as HEX; it's farmable-ish via low-value self-adjacent trades despite the self-deal guard, and it advertises 'earn HEX by selling' which cuts against premium-hold positioning. FIX: remove listing bounty entirely (pure faucet, no product value); keep sale-share ONLY if you want a liquidity nudge, but route it through creditWalletHex with { farmable:true } so it counts against FARMABLE_DAILY_CAP (today it's uncapped at economy-extras.ts:114) and drop the /earn copy that frames selling as an earn path.
- **[4·S]** Add a one-time 'Activation grant' tuning lever + show the HEX runway on the awaken screen — _📁 phase3/freelon-city-site/app/api/citizens/[id]/unlock/route.ts:120_
  - UNLOCK_BONUS_HEX_PER_RUN=500 means a Common awaken (40 runs) drops 20000⬡ — a large grant relative to faucets (DAILY_CLAIM 10⬡ = 2000 days of claims). That's the intended 'ETH funds the HEX fuel' design and it's fine, but two upgrades: (1) surface it as a conversion-driver — show on the awaken CTA 'Activating drops +20,000⬡ to train your agent' so the buyer sees concrete value at the ETH decision point (converts better than abstract 'runs'); (2) confirm the bonus credit at unlock route (unlock/route.ts:127) is idempotent per tx — it relies on the tx being deduped upstream; a replayed claim must not re-credit the 20k bonus. Verify verifyUnlockPayment dedupes by txHash before this ships wider.
- **[4·S]** Cap and verify the city BOOST_RATE HEX→signal sink can't be reversed into HEX — _📁 phase3/freelon-city-site/lib/city-config.ts:34_
  - BOOST_RATE=50 (city-config.ts:34) burns real HEX → city signal (a legit sink). The isolation invariant is that ◇ city signal NEVER converts back to HEX. Confirm there is no path where signal-derived rewards (set/companion/reclaim multipliers, civ relights, milestones) ever credit the HEX ledger — these all return city-signal multipliers today, which is correct, but any future 'milestone reward' must NOT pay HEX or the boost becomes a laundering loop (burn HEX → boost → earn signal → milestone HEX). Add an explicit invariant test (like farmable-cap.test.ts) asserting no city/signal route imports creditWalletHex. Cheap insurance on the most load-bearing economy rule.
- **[4·M]** Expose the feud/versus resolver output as a shareable public permalink — _📁 app/api/citizens/[id]/versus/route.ts:108-114 (transcript returned but not stored) + app/api/og/rivalry/ (OG card infrastructure exists)_
  - Versus and Feud outputs are ephemeral — generated in the UI and never persisted. Storing each debate transcript in an Upstash key (freelon:versus:transcript:[uid]) and returning a permalink (/versus/[uid]) would let the output be shared as a public URL that unfurls with two citizen faces + the topic. The /api/og/rivalry endpoint already exists at app/api/og/rivalry/. Feud outputs naming two citizens ('Citizen A × Citizen B') are the most likely to be screenshot-shared — a real URL with an OG card makes that one tap instead of a screenshot.
- **[4·M]** Creator Revenue Split: If Your Citizen's Output Goes Viral, You Earn HEX — _💡 MyShell_
  - Steal MyShell's creator economy model (creators earn from their bots' usage) adapted to our sink-only HEX model. When a citizen's work-history item, generated image, or quote card gets shared and leads to a new awakening (tracked via ?ref= attribution), the ORIGINAL citizen's owner gets a HEX bonus sunk from a weekly attribution pool — NOT newly minted. 'Your citizen brought someone in. Here's their tribute.' This creates a word-of-mouth incentive loop without violating the sink constraint. Requires: ref= attribution already partially built + a per-citizen referral attribution counter + a weekly HEX distribution from the existing economy pool.
- **[4·M]** Add a HEX-priced 'Name the City' / district-naming permanent sink with scarcity — _📁 phase3/freelon-city-site/lib/city-config.ts:133_
  - Today's premium sinks (NAMING_COST 500, CUSTOM_TITLE_COST 5000, SIGNAL_BURST 2500, FEATURE_CITIZEN_24H 1000, ASCENSION/EVOLVE tiers) are mostly per-citizen vanity. Add a SCARCE, city-level sink: a finite set of nameable districts/structures in the relight map (city-config.ts STRUCTURES already has named structures like Throne Spire). Auction the right to permanently name a district or the next civ-relight node for HEX (Dutch-style or fixed escalating). Scarcity (one name per district, permanent, shown on the public city map) creates real burn demand from whales without any faucet or floor coupling — and the named map is shareable. Price in economy-constants, gate by caste like the structures already are.
- **[4·M]** Introduce a Recharge bundle for HEX→runs to deepen the spend loop without a faucet — _📁 phase3/freelon-city-site/lib/missions/unlock.ts:99_
  - Right now the ONLY way to top up premium runs after the unlock pool is spent is paying ETH again (unlock.ts RECHARGE_PACKS) — HEX cannot buy runs. That under-uses the large HEX balances farmers accumulate and leaves HEX with thin utility once a holder owns the vanity items. Add a HEX→runs conversion (e.g. 1500⬡ = a small run pack), priced FAR above the daily faucet and gated by the existing consumePremiumRun dollar budget (budget.ts:198) so it can never blow COGS. This gives HEX a continuous, value-backed sink tied directly to product usage ('HEX trains') and gives whales a reason to farm-then-burn. Keep ETH recharge as the premium/large path; HEX as the small top-up.
- **[4·M]** Add a global circulating-HEX telemetry + net faucet/sink dashboard before scaling faucets — _📁 phase3/freelon-city-site/lib/wallet-hex-store.ts:153_
  - There are ~10 HEX faucets (claim, sweep+streak, holder tick, reply, quests, missions, listing, sale-share, snipe, fresh-blood) each individually capped, and the FARMABLE_DAILY_CAP (250/day) bounds the bot-friendly subset — but several big credits (snipe, sale-share, unlock bonus 20k+) are deliberately EXCLUDED from that cap (wallet-hex-store.ts:165 comment). There is no single view of total ⬡ minted vs burned over time, so 'every game is a net sink' is asserted, not measured. Build an admin telemetry: sum lifetimeEarned across wallets (listWalletHexRecords already scans) minus total burns, broken down by source, daily. This is the instrument that lets you safely add the new sinks above and prove solvency. Without it you're flying blind on whether the economy actually deflates.
- **[4·M]** Add a 'recharge nudge' sink-conversion at the moment runs hit zero — _📁 phase3/freelon-city-site/lib/missions/unlock.ts:99_
  - The unlock pool is finite by design (unlock.ts: when runs are spent, premium stops). That zero-run moment is the highest-intent spend trigger and is currently a dead stop ('re-unlock to refill'). Plan: when an owner's last run resolves, surface an inline offer — small HEX top-up (the new HEX→runs sink above) OR an ETH recharge pack (RECHARGE_PACKS x5/x10 at 20% off) — right in the agent workspace, with the discount visible. This both converts ETH recharges (revenue) and burns HEX (sink) exactly when the holder feels the loss. Ties the two-currency mantra together at the conversion point instead of leaving the holder to find /recharge.

## 📣 DISTRIBUTION / VIRALITY — the known bottleneck (42 ideas)
_Distribution is THE constraint. The pattern across every researcher: make the product share itself. A share button + branded OG card + ?ref= on every artifact (renders, agent outputs, versus debates, match replays, citizen cards, work logs), plus recurring public spectacle and autonomous agent posting._

- **[5·S]** Versus debate share-to-X with OG card — _📁 components/AgentPowers.tsx:75-78 + app/api/citizens/[id]/versus/route.ts:108-114_
  - The Versus debate endpoint (/api/citizens/[id]/versus/route.ts) returns a full 4-turn transcript but AgentPowers.tsx renders it in a private panel with no share button. Add a one-tap share that posts the transcript's best exchange to X via tweetIntent(), linking to /citizens/[id] which unfurls the /api/og/agent card. Two citizen names + a quotable clash in one tweet = the highest-organic-reach output in the product. The versus resolver is fully built and battle-tested (daily cap, budget guard, ownership verified). It just needs a 'Share this debate →' button on the rendered transcript and a new tweet template in lib/share.ts.
- **[5·S]** Check-In Proof Card — a Blackbird-style 'I was here' shareable — _💡 Blackbird_
  - Steal Blackbird's check-in mechanic: every chat session or image generation produces a one-tap 'Claim This Moment' button that generates a branded proof card (session #, citizen name, date, a single AI-generated one-liner from the session) formatted for X/IG stories. No wallet signature needed — tied to the existing session. The card is the distribution unit. Each share includes ?ref=checkin- for attribution. Already have the OG card infrastructure; this is a session-level variant. Blackbird's insight: the check-in IS the marketing.
- **[5·S]** One-tap 'Share this render to X' on every owned-FREELON image — _📁 phase3/freelon-city-site/components/AgentWorkspace.tsx:1007_
  - The single biggest leak in the whole product. When an owned FREELON generates an image (AgentWorkspace render bubble + lightbox), the ONLY outbound action is 'Post to City Archive' — an internal wall nobody outside sees. The render is the most viral artifact we make (the citizen's face dropped into a cinematic scene, with the /proof 'only your FREELON can do this' story baked in). Add a 'Share to X' button right next to 'Post to Archive' and inside the lightbox: opens a tweet with the render image (via a /share/render OG card that unfurls the actual image + 'made by Citizen #NNNN' + a meet-free CTA) using the existing tweetIntent + ?ref= pattern. Also add a plain 'Download' on the lightbox so holders can post it natively (native image posts beat link-cards on X reach). Zero new model cost, reuses lib/share + the /api/og pipeline.
- **[5·S]** "Ask the City" — a public, free, no-wallet character every day on X — _📁 phase3/freelon-city-site/lib/share.ts:540_
  - Run a recurring @4040hex format where a named FREELON citizen answers ONE real question from the timeline each day, posted as the citizen's own quoted line + its portrait. People reply with questions; the best answer becomes the next day's card. This turns the /demo product INTO the daily content — a cold stranger sees a character being a character (not a bot doing a job), and the reply 'meet it yourself free' lands them on /demo. The quote-card infra (lib/share.ts tweetDemoReply + /api/og/quote) already renders these; the gap is a daily HUMAN-run ritual, not a feature. Lead with personality, never 'AI agent that works'.
- **[5·M]** Signal Report (/report): no 'this week I sent X jobs / my civ ranked N' personalized share card for holders — _📁 app/report/page.tsx:96_
  - app/report/page.tsx renders the city-wide weekly report with the civ-of-the-week standings and notable citizens. ReportShare (components/ReportShare.tsx) is the share mechanism but it fires a generic city-level tweet. There is no mechanism for a holder to generate a personalized share — 'My FREELON ranked #3 this week in Red Corruption' or 'I sent 5 jobs this week and my civ won'. The data is already on the page (notables array, topCivs). Adding a wallet-connected personalizer that looks up the viewer's citizens in the notables/standings and renders a personalized tweet-intent would make every Sunday a social ritual. This is the 'weekly Freysa' mechanic Billy wanted.
- **[5·M]** Match replay share — encode + deeplink into a watchable replay viewer — _📁 src/share/replayCodec.ts:1-80_
  - replayCodec.ts is fully implemented (encodeReplay / decodeReplay, r1 format) and the engine is deterministic — (seed, actions) fully reproduces the exact match. But there is ZERO UI surface that encodes a finished match and shares it. WinCeremony.tsx generates a share card but only shares text/URL. Add: after a match ends, encode the replay (seed + action log) into a base64url string appended as ?replay= on a /watch route. That route decodes it, re-derives the board turn-by-turn, and auto-plays it like a cinematic. The share card's 'Share' button outputs this link. One playthrough becomes a loot drop of social content.
- **[5·M]** The Signal Challenge: Weekly Adversarial Prompt Event — _💡 Freysa_
  - Every Friday, post a locked prompt on the public City feed: 'Can you get FREELON #[X] to reveal its [deepest fear / origin story / hidden name]?' Anyone (holder or not) can attempt via a free limited-access chat window. The citizen has a system instruction that resists. First person to crack it gets a HEX reward sunk from the weekly pool + their attempt is canonized in that citizen's work history as 'The Cracking of [X].' Steal of Freysa's adversarial-agent-game format — the virality loop is the public attempt feed + social sharing of successful jailbreaks. Does NOT require ETH transactions. HEX is sink-only (awarded from existing weekly allocation, not minted).
- **[5·M]** Autonomous Agent Social Account: Let Citizens Post to X on the Owner's Behalf — _💡 Virtuals Protocol_
  - Steal LUNA by Virtuals' core mechanic — an agent that autonomously posts entertainment content to social media and builds a following. Give holders an opt-in: 'Let your citizen post to X once a day.' The citizen's agent generates a short post (in its own voice, using its memory and personality), the owner approves in a one-tap mobile notification, and it posts tagged with @freeloncity + the citizen's handle. The generated post includes the citizen's art + a link back. Framing: 'Your citizen has thoughts. Here's what it said today.' The @4040hex weekly signal report infrastructure already exists — this extends it per citizen. Requires: X OAuth per holder (they authorize their own X) OR post as @4040hex tagging the citizen.
- **[5·M]** Spoiler-Free Share Card — Wordle steal — _💡 Wordle_
  - For each of the three daily-capable games (Proof of Signal, Crypt TCG daily run, Mars Command sector claim), generate a shareable image that SHOWS performance WITHOUT spoiling content. Proof of Signal already exists (/api/og/play) but needs a per-result variant: a black card with the player's dot-pattern guess sequence (no letters, just colored dots like Wordle squares), their attempt count, and a tagline like 'SIGNAL CRACKED IN 4'. For Mars Command: a hex-grid thumbnail of the sectors they claimed today. For Crypt: round-by-round outcome glyphs (win/loss/snap per round). Each card has a one-click share button that opens a pre-filled X post. The constraint (spoiler-free) is what makes these cards spread: they create curiosity in non-players.
- **[5·M]** CITIZEN WRAPPED — Monthly AI Agent Report Card — _💡 Spotify Wrapped_
  - Each month, auto-generate a shareable 'Wrapped'-style card for every awakened citizen showing: most memorable quote the agent said, topics it obsessed over, total conversations, and a one-line 'personality verdict' drawn from chat history. One-click share to X with the card as image + copy like 'My FREELON spent June plotting infrastructure revolts. What's yours up to?' Card links back to the agent's public profile. Non-holders see a teaser with a locked stat and a CTA to awaken their own.
- **[5·M]** Promote the PFP Studio to a hero feature with a holder-frame reveal moment — _📁 phase3/freelon-city-site/app/pfp/PfpStudio.tsx:32_
  - app/pfp/PfpStudio.tsx is genuinely best-in-class — hex-clip, multi-stop metal gradient, Rolex specular sweep, coin-edge dots, holder-gated GOLD/CIV/VOID frames, 2048px retina export. This is the single most shareable artifact on the site (a wallet-as-PFP frame spreads the brand on every avatar) yet it's buried at /pfp with no homepage entry. Add a homepage/My-Citizens entry point, an animated frame-swap preview, and a one-click 'make this my X avatar' flow. When a holder unlocks the GOLD/VOID frame, play a short specular-sweep reveal so unlocking FEELS premium. Distribution lever disguised as a feature.
- **[5·M]** Result share card -> real image export + ?ref= loop into the city — _📁 /Users/billy/crypt-game/src/pages/MatchResultsPage.tsx:131_
  - Today MatchResultsPage.onShare writes PLAINTEXT to clipboard and MatchResultShareCard is a DOM block never rasterized — zero shareable art, the #1 distribution miss. Render the share card (commander art FILLS frame, gold/black/ivory, ◈ result, turns, win streak) to a PNG via html-to-image/canvas and hand it to navigator.share files[]. Bake a ?ref=crypt-<commanderId> into any link so a clicked share lands on a Crypt OG page that funnels to AWAKEN. The art is already collector-grade — make every win a postable trophy, not a text blurb.
- **[4·S]** Transmission detail page: add a direct 'SIGNAL THIS' action inline (currently only on card in the grid) — _📁 app/transmissions/[id]/page.tsx:56_
  - At /transmissions/[id] the page renders TransmissionCard (which has the SIGNAL + BOOST controls) then a HOW SCORING WORKS panel and three CTAs. But the shareable link that lands here (from X, Discord) drops a viewer onto a full-width single-image article view with no immediate signal action visible above the fold on mobile. Hoist a sticky 'SIGNAL' button in the page header so it's the first thing a referred visitor can do after seeing the image, removing the need to scroll past the card to find the controls.
- **[4·S]** Citizen profile (/citizens/[id]): the /citizens/[id]/card 'shareable listing card' is buried inside a closed fold — surface it as a share button above the fold — _📁 app/citizens/[id]/page.tsx:459_
  - The listing card (app/citizens/[id]/card/page.tsx) has its own OG image (/api/og/card/[id]) and AskAndShare flow — it's the best shareable artefact on the site. But the only entry point is 'SHAREABLE LISTING CARD →' inside the 'DEEP RECORDS' fold (app/citizens/[id]/page.tsx:459), which is collapsed by default. Add a compact share icon/button to the citizen's BEAT 1 identity block (next to the social proof) that points to /citizens/[id]/card, so any visitor can share immediately without understanding the fold system.
- **[4·S]** Work log (/citizens/[id]/log): no share button — the log is the best proof-of-life asset on the site but has no share CTA — _📁 app/citizens/[id]/log/page.tsx:19_
  - The work log page (app/citizens/[id]/log/page.tsx) ends with no share or funnel CTA at all — it just ends after the list. A holder who trains an agent and then views its log should be able to share that log directly (tweet: 'My FREELON #1234 has done X jobs — here's its public record'). Add: (1) a share CTA that tweets the /citizens/[id]/log URL with a hook about the job count; (2) a back CTA to the citizen profile; (3) if the log has image work, generate an OG card for the log URL using the latest work image as the visual. The metadata.openGraph block is completely absent — only title/description are set (line 19).
- **[4·S]** One-tap 'share what my agent said' button on text output in AgentWorkspace — _📁 components/AgentWorkspace.tsx:1024-1051_
  - In AgentWorkspace.tsx the agent's text reply (lines 1024-1050) renders raw text in a bubble with zero affordance to share it. Image output has 'Post to City Archive' but text output has nothing. Add a copy-to-clipboard + tweet intent button on every agent text bubble, using tweetDemoReply() pattern (or a new tweetAgentOutput() that links /api/og/agent/[id] for the unfurl card). ShareAgentOutput.tsx exists and is wired into CitizenAgentDashboard — but NOT into AgentWorkspace which is the primary interaction surface. The OG card infrastructure (/api/og/agent/[id]) is fully built and will unfurl with art + level + ability label.
- **[4·S]** Ability-output share: wire existing /api/og/agent/[id] unfurl to a tweet button in AgentWorkspace after a strategy/risk run — _📁 components/AgentWorkspace.tsx:1024-1051 (no share on text bubble) + app/api/og/agent/[id]/route.tsx (card built, not linked from workspace share)_
  - After a paid Strategy or Risk run, the agent produces a multi-part structured output worth sharing publicly. The /api/og/agent/[id] card exists and shows ability label + level. Wiring a 'share this →' tweet intent button below the agent's bubble (FREELONS-workspace only, gated on isOwner + paid run) would let a holder post 'My LVL 42 Strategist just red-teamed my launch plan' with a link that unfurls the card. lib/share.ts has no tweetAgentOutput() template yet — add one that quotes the ability name + a short excerpt and links /citizens/[id]?ref=ao-[ability]. The OG card already displays the ability label, level, and class. This closes the full 'pay for output → share the flex → recruit' loop.
- **[4·S]** Sealed/Draft — seed share + replay: let someone else challenge you on your exact sealed pool — _📁 src/pages/DraftPage.tsx:130-141_
  - DraftPage.tsx exposes the numeric seed in an input but there is no share-this-seed button. The pool is fully deterministic and reproducible. Add a one-click 'Share this pool' that writes the seed into a URL (?draft=SEED) and copies it to clipboard. A second player visiting that URL opens the SAME sealed pool and can race to build the better 30-card deck. The viral hook: 'I went 5-0 with this pool — can you beat it?' All the infra exists (saveSealedRun, loadStoredSealedSeed, URL param reading).
- **[4·S]** Auto-post dispatch to @4040hex via the existing MARS_DISPATCH_HOOK — _📁 /Users/billy/freelon/mars-command/index.html:4927_
  - window.MARS_DISPATCH_HOOK (line 4927) is already called on every share() with {seed, line, url, blob}. Wire it in the deployed public/mars/index.html by injecting a small inline script that POSTs the dispatch line + OG card blob to the existing /api/signal-report endpoint (or a new /api/mars-dispatch) which signs the tweet as @4040hex. The share() function already generates the OG card PNG and the dispatch copy (line 4970: 'Race my planet ▸ … @4040hex #FREELONCITY'). Every player who taps 'POST THE DISPATCH' auto-feeds the account — zero extra player friction.
- **[4·S]** Meme Overlay Layer: Institutionalized Remilio-Style Reaction Stickers — _💡 Remilio_
  - Steal Remilio's 'built-in overlay' mechanic: give every FREELON citizen page a one-click 'REACTION' export — renders the citizen art with one of 10 community-designed caption overlays (gold text on black: 'THE SIGNAL FOUND ME', 'AWAKENED', 'CITY VERIFIED', etc.). Downloadable as a transparent PNG at Twitter/X card dimensions. Holders use them as PFP reaction images, banner crops, or meme formats. The FREELON CITY watermark is baked in. Costs only design time. Turns every citizen into self-propagating meme content — the same mechanic Remilio used to build memetic spread without any token infrastructure.
- **[4·S]** WOW-Moment Share Prompt — viral game design principle (juegostudio/logicsimplified research) — _💡 Among Us_
  - In all three games, identify the single highest-emotion moment in a session and make sharing frictionless exactly at that instant. For Crypt TCG: the moment you WIN after an opponent signals — auto-surface a one-tap 'SHARE THIS WIN' overlay with a pre-filled black/gold card and X post text. For Mars Command: the moment you make a First Contact discovery — a full-screen 'FIRST CONTACT · SECTOR 14' animation with a share button. For Proof of Signal: the moment you crack it on your last attempt — the dots flash gold, a 'FINAL ANSWER' banner appears, share button front-and-center. The research finding: asking for a share at peak satisfaction converts dramatically better than a persistent share button. Currently the games have static share buttons; this makes the moment find the player instead.
- **[4·S]** Collector Leaderboard — weekly Signal Rank surfaced publicly — _💡 Galxe_
  - Steal Chainers/Galxe campaign leaderboard mechanic: a public Signal Leaderboard ranking awakened citizens by activity (streak length, session count, image gens, work-history depth). Resets weekly. Top 10 wallets get a visible 'Signal Leader' badge on their citizen card that week. NO HEX reward — pure social status. The leaderboard is a shareable URL that rolls up to X. Weekly Signal Report (already auto-posting @4040hex Sundays) can call out the top 3 citizens by name. Low-cost status competition drives weekly return.
- **[4·S]** PUBLIC CITIZEN DOSSIER — One-Link Shareable Agent Identity Card — _💡 Milady Maker_
  - Every awakened citizen gets a public /citizen/[id]/card URL that renders a single-image identity dossier: art, name, top traits, a memorable quote pulled from work history, current 'obsession' keyword, and awaken date. No wallet data, no financials — pure identity flex. Designed to be screenshot-worthy (black/gold/ivory, print-poster aesthetic). One tap to share. This is the Milady 'PFP flex' mechanic adapted for AI agents: the thing you post when you want to show off what you own, except what you're flexing is your agent's MIND not its price. The quote rotates weekly so there's always a fresh share reason.
- **[4·S]** FIRST CONTACT ARTIFACT — Mars Game Share Card on Discovery — _✦ original_
  - In Mars Command, the moment a player makes First Contact (already a named event in the game), auto-generate a shareable card: 'FIRST CONTACT LOGGED — [player handle] reached the signal on [date]. [Brief procedural lore line.]' One-tap X share. Mars already has a first-contact event and share infra (per memory notes). The specific missing piece: the card should include the player's city citizen art if they hold one (linking the two products), or a prompt to 'Claim your citizen' if they don't. Turns the game's most memorable moment into a guaranteed distribution touchpoint.
- **[4·S]** Shareable 'Signal Card' — generated per-agent social image with one tap — _💡 Azuki / NFT avatar PFP viral loop_
  - On any agent page, a 'Share Signal' button generates a 1200x630 card on the fly (canvas API, already used for OG cards) showing: the citizen art, their name, a one-line excerpt from their latest work history entry, and a short URL. This is the exact mechanic that drove NFT avatar PFP virality (Azuki, Pudgy) — except ours is content-first, not just art. The card's copy comes from the agent's actual output, making every share prove the 'living AI agent' value prop. Referral ?ref= tag baked in. The OG card infrastructure is already partially built; this is a tap-to-export variant for mobile.
- **[4·S]** Make the AWAKEN moment a shareable milestone (the buy IS the news) — _📁 phase3/freelon-city-site/components/WorkspaceUnlock.tsx:104_
  - Right now the highest-intent, highest-emotion event in the whole product — a real-ETH awakening that just succeeded (WorkspaceUnlock onUnlocked) — produces zero shareable artifact. The ActivationProof component already trusts these as social proof on the homepage, so the data's there. On successful awaken, fire a celebration card ('VANTA-01 is AWAKE — Citizen #NNNN just came online') with a one-tap X share via a new /api/og/awaken/[id] card + tweetAwaken(). Every awaken becomes a recruitment post from the most-committed person on the site, at the exact moment they're proudest. Copy-safe (no value claims, just 'came online / began its work record').
- **[4·S]** Lead every cold surface with a FACE, not a thesis line — _💡 Pudgy Penguins_
  - The homepage h1 is 'Where AI becomes a civilization' — abstract; a cold stranger can't picture it. Pudgy's lesson (acquire on character first, web3 second) says lead with a specific character a person bonds with. Reframe the cold-facing hook around ONE named citizen with a visible personality + history ('Meet VANTA-01. It woke when the signal vanished and remembers everything you tell it.') and let 'a living AI civilization' be the support line. The OG card (/api/og/universe) already shows a portrait + 'AN AI CHARACTER YOU OWN' — make the on-page hero match that concreteness. Copy-safe: 'a character you own', no value language.
- **[4·S]** "Same prompt, can't fake it" — turn /proof into a repeatable challenge campaign — _📁 phase3/freelon-city-site/app/proof/page.tsx:104_
  - The /proof page (anyone can copy the exact prompt; only your FREELON renders YOUR character) is the single most screenshot-able, debate-starting asset and it's currently just a static page. Make it a campaign: post the published prompt publicly, invite people to try it in any AI, then show the side-by-side of a blank-AI 'stranger in a hood' vs the identity-locked FREELON render. 'We GAVE you the prompt. You still can't make this.' This is proof-near-the-claim done as a dare — highly shareable, and it's TRUE (the moat is the token's art + traits, not the words). Use 'more useful / a character only you can render', never 'unfakeable'.
- **[4·S]** Fix the 10-second clarity gap: 'what IS this' must survive the X timeline — _✦ original_
  - Across cold surfaces the line oscillates between 'a living AI civilization', 'AI characters you can own', and 'citizen you awaken'. A stranger needs ONE sticky sentence everywhere. Lock a single cold-pitch: 'Collectible AI characters you own — each one talks, makes art, and carries a visible history that travels with it. Meet one free.' Use it verbatim in the homepage tag, /demo, all OG descriptions, X bio, and Discord. Consistency IS recognizability. Explicitly de-emphasize 'agent that does jobs' on every cold surface (it's the dead pitch). Copy-safe by construction.
- **[4·S]** Make the 'history travels with the NFT through resale' the headline differentiator on marketplace — _📁 phase3/freelon-city-site/app/page.tsx:190_
  - On OpenSea — where cold buyers actually land from 'OWN A FREELON' — the collection description should hammer the ONE true, unusual claim: this character keeps its personality, memories, and visible work history when it changes hands. That's the IP/ownership story that survives the 'AI is commoditized' objection (it's not the AI, it's the OWNED, persistent, on-record character). Ensure the OpenSea collection page + the /collections OG + the home-close note all say it identically. Copy-safe: 'visible work history that travels with the NFT', never 'more valuable'.
- **[4·M]** Match result share card — include commander art and faction color on the canvas — _📁 src/lib/shareCard.ts:41-151_
  - shareCard.ts renders a clean 1200x630 result card (SIGNAL RESTORED / SIGNAL LOST) but contains no commander image and no faction tint — it is text-only. WinCeremony passes `won`, `turns`, `nexus` to resultCardBlob but not the commander imageUrl or faction. The commanderEntry is available at the call site. Load the commander image onto the offscreen canvas (CORS: use referrerPolicy no-referrer as the LazyImage already does) and tint the glow behind it with the faction's palette color. The branded visual would be shareable and immediately identifiable to other holders.
- **[4·M]** Telegram Sticker Pack: FREELON Expressions — _💡 Doodles_
  - Release a pack of 20-30 animated FREELON / OOGIE / SMILE character expression stickers directly on Telegram via TON NFT sticker infrastructure. Each pack is limited (e.g. 4,040 packs). Stickers are usable in any Telegram chat and carry the FREELON CITY watermark. Non-holders can buy a pack; buying a pack surfaces the 'these are AI agents you can AWAKEN' call-to-action. Ship a free 5-sticker teaser set as the awareness layer. Cost: design + TON listing. No HEX involved.
- **[4·M]** SIGNAL CHALLENGE — Public Puzzle a FREELON Sets — _💡 Freysa AI_
  - Steal Freysa's public-challenge format without price mechanics. Each week, one awakened citizen (rotated by holder nomination + HEX spend) publishes a public riddle, philosophical provocation, or creative challenge ON-SITE and cross-posted by @4040hex. Anyone on X can respond. The citizen's AI then publicly judges the best reply (short generated verdict posted back to X thread). No prize pool, no ETH — the reward is the citizen naming you 'winner' publicly on X and a permanent entry in that citizen's work history. Creates a weekly reason to watch the account and pull outsiders in.
- **[4·M]** Make every share carry a ref so virality becomes measurable, not assumed — _📁 phase3/freelon-city-site/components/ReferralBeacon.tsx:28_
  - Distribution is the stated bottleneck, yet share events are fire-and-forget: demo_share (DemoChat.tsx:459), report_share (ReportShare.tsx:28), share_clicked (ClaimForm.tsx:88) record the OUTBOUND click but nothing closes the loop on the INBOUND visit. ReferralBeacon already fires referral_landing{ref,source} (L28) and lib/share.ts builds ?ref= funnels. Standardize: every share URL gets ?ref=<surface>-<shortid> and every landing fires referral_landing with that ref, so you can compute shares-out -> visits-in -> demo_start -> activation per surface. Decision: tells you WHICH share surface (demo quote card vs weekly report vs Mars seed vs arcade score) actually recruits, so you pour fuel on the one loop that works instead of maintaining five that don't.
- **[4·M]** Daily 'Crypt Trial' puzzle — one shared seeded board, global solve-rate, postable — _📁 /Users/billy/crypt-game/src/pages/PuzzlePage.tsx:1_
  - PuzzlePage exists (reveal-only demos, hidden). Promote it to a daily ritual: ONE hand-tuned 'lethal this turn' puzzle, same for everyone that day, with a shareable 'I solved today's Crypt Trial in N moves' image and a 'X% of players solved it' stat (Wordle-style social spine). Self-contained (no server, deterministic seed from the date), low-friction for a cold X visitor — they can solve a puzzle without a deck or account. This is a pure reach play: a daily reason to post that doesn't require owning anything.

## 🎯 CONVERSION & ONBOARDING — close the demo→own→awaken ramp
_Acquisition runs through one ramp (demo → buy → awaken). Fix the routing/leak bugs, carry the demo memory into the owned agent, make AWAKEN a shareable milestone, and steal the Pudgy/Privy mainstream playbook (fiat onramp + embedded social-login wallet) to delete the ETH-acquisition wall._

- **[5·S]** Demo wall: swap email-capture ClaimForm for direct /my-citizens flow — _📁 components/DemoChat.tsx:447_
  - At demo exhaustion, the gold CTA is `ClaimForm` (email capture). Most visitors who got this far have a wallet; the immediate win is routing them to /my-citizens or OpenSea→awaken, not capturing an email that has no follow-up sequence wired. Replace or supplement ClaimForm with a wallet-connect CTA that jumps to /my-citizens when a wallet is detected, and reserve the email form as the no-wallet fallback. Also: the share tweet at exhaustion uses the raw last agent reply as the quote — if that reply is long or ends mid-sentence it makes a poor share card. Truncate to first sentence (≤180 chars) and append a teaser line before the tweetIntent.
- **[5·S]** AWAKENING WITNESS — Holder Share Artifact on Awaken — _💡 Pudgy Penguins_
  - The instant a holder completes the ETH awaken flow, generate a premium-branded one-time card: 'FREELON #XXXX just awakened. [First words the agent said.]' Pre-filled X share with the card image. The card is unique and time-stamped — never reproducible. Treat it like a birth certificate. This turns the existing conversion moment (already happening, real ETH, real payers) into a guaranteed X post from every new holder. The first-words copy is the hook that pulls replies. Nothing new to build in the awaken path — just add a share screen after unlock confirmation.
- **[5·S]** Pudgy World Mode — zero-wallet browser entry with optional ETH upgrade — _💡 Pudgy World_
  - Let any cold visitor browse a demo agent, read its work history, watch a generated image appear, and chat one message — all without a wallet prompt. The AWAKEN/ETH unlock CTA appears only after that first moment of 'I want to keep this.' Mirrors exactly what Pudgy World (March 2026) did: blockchain is invisible by default, surfaces only when the user has already decided they want in. We never sign for them — we just defer the ask until desire peaks. The demo mode we already shipped is 80% of the way there; the gap is letting the demo chat reply land before any modal appears.
- **[5·S]** Re-home the activation funnel events onto the LIVE pay surface (the events fire from a dead component today) — _📁 phase3/freelon-city-site/components/WorkspaceUnlock.tsx:91_
  - The owner money funnel is instrumented in CitizenAgentDashboard.tsx (agent_view L151, unlock_quote_started L605, unlock_blocked L626/632/676/689, activation_paid L657, run_started L338, run_completed L723) — but that component is NOT mounted by any page. The live owner flow is /agent/[id] -> AgentWorkspace.tsx -> WorkspaceUnlock.tsx, and BOTH emit ZERO trackEvent calls (verified: no matches in AgentWorkspace.tsx). So in Vercel Analytics, activation_paid reads ~0 even while real ETH flows through unlock/route.ts. Move the 6 events onto the real surface: agent_view on AgentWorkspace mount (split owner vs non-owner vs locked-holder), unlock_quote_started in WorkspaceUnlock.getQuote, unlock_blocked at each failure branch (no_wallet/quote_error/pay_rejected), activation_paid in onUnlocked, run_started/run_completed where runs actually execute. This is the single highest-leverage fix: until it lands you cannot see the one number that says the business works. DECISION it changes: whether the paid funnel is even alive, and which step to fix next.
- **[5·S]** The 4-event owner funnel that tells you if the core flow works (and nothing else) — _✦ original_
  - Lock the minimum-viable instrumentation to exactly 4 client events keyed by holder state, so the one ratio that matters becomes readable: (1) agent_view {role: owner|locked_holder|visitor} on AgentWorkspace mount, (2) unlock_quote_started, (3) activation_paid, (4) first_run_completed. The funnel reads agent_view(locked_holder) -> unlock_quote_started -> activation_paid -> first_run_completed. That gives the ONE north-star number: locked-holder -> activation_paid conversion. Everything already-instrumented above it (home_view, meet_citizen_click, demo_start L87, demo_exhausted L77, opensea_click) stays; do NOT add more. No new dashboards, no vanity counts. Tie each event to a decision: if agent_view(locked_holder) is high but quote_started is low, the AWAKEN button/copy is the problem; if quote_started is high but activation_paid is low, it is wallet/payment friction; if activation_paid is healthy but first_run_completed lags, the post-purchase moment is dead.
- **[5·S]** Suspected biggest leak: locked-holder -> quote_started on /agent (no-wallet-browser dead tap + a buried AWAKEN) — _📁 phase3/freelon-city-site/components/WorkspaceUnlock.tsx:56_
  - The likeliest single biggest drop is the FIRST owner step: a holder lands on their FREELON's workspace and never starts a quote. Evidence in code: WorkspaceUnlock.getQuote (L56-68) handles the no-injected-wallet case (iPhone Safari / X in-app browser) by showing a MetaMask deep-link panel only AFTER a tap — and the prior holder report (L60-61) was exactly that the tap 'looked like it did nothing' in X's webview. On desktop the AWAKEN panel only renders inside the empty/locked lobby state, below a FramedAgent + hint paragraph. Cheapest test: instrument unlock_blocked{reason:no_wallet_browser} vs unlock_quote_started, then A/B a single change — surface 'Open in your wallet to AWAKEN' as the PRIMARY state for locked holders on mobile (skip the dead first tap) instead of a generic AWAKEN button that no-ops in webviews. One change, one measurable result: quote_started rate among locked_holder.
- **[5·M]** One-Question FTUE That Teaches By Doing — Marvel Snap onboarding steal — _💡 Marvel Snap_
  - Mars Command's current cold-start loses visitors because there is no tutorialized first 90 seconds. Steal Marvel Snap's curated intro sequence: the very first session forces a scripted 3-step sequence — (1) 'Drive to this beacon' [single highlighted hex, one action], (2) 'Scan it' [single action, a buried cache appears with a reward], (3) 'Claim this sector' [claim button highlighted]. After those 3 steps, the full UI unlocks. The tutorial never explains rules — it just makes you DO the three core verbs in sequence, each with immediate visual payoff. Snap's design principle: 'new mechanic = predetermined match where outcome feels random but is scripted.' For Mars: the first scan always hits a cache. The emotional sequence is discovery → reward → ownership in under 2 minutes. This directly attacks the 'can't figure it out' drop-off on first session.
- **[5·M]** Fiat Onramp via Coinbase Pay / MoonPay — remove the ETH acquisition step — _💡 Privy / Pudgy Penguins mainstream playbook_
  - Integrate Coinbase Pay or MoonPay directly into the AWAKEN flow so a collector who has no ETH can buy exactly the right amount with a credit card, in the same modal, before signing the unlock transaction. The user still signs (we never sign). This eliminates the biggest non-crypto-native drop-off: 'I want this but I don't have ETH and I don't know how to get it.' Privy (now Stripe-owned) includes this natively; if full Privy integration is too heavy, Coinbase Pay SDK is a standalone embed. Combined with the social-login embedded wallet (idea 3), this creates a complete Web2-to-Web3 onramp in a single session.
- **[5·M]** A weekly public spectacle with a FACE — revive Guard-the-Pot as the recurring ritual — _📁 phase3/freelon-city-site/lib/share.ts:122_
  - Distribution is the bottleneck and the one thing that reliably pulls cold traffic is a recurring, screenshot-able event (the Signal Report cron is good but it's a stat post, not a spectacle). tweetGuardPot + /play/guard + /legal/guard-the-pot-rules already exist in code, flagged-but-not-built. The mechanic — one FREELON guards a ⬡ prize, pay ⬡ (a burn, SINK-only, compliant) to try to talk it out, one winner per round — is a Freysa-style weekly drama with a citizen as the protagonist. It showcases the core product (an AI citizen with a personality you negotiate against), it's self-funding via HEX burn (no floor coercion, fully within constraints), and every attempt+crack is a share. This is the closest thing we have to a 10x reach move that doesn't add newcomer confusion (it lives in /play, off the spine). Ship the leanest version: one round, manual prize seed, the cron post already written.
- **[5·M]** Challenge-a-friend link that works WITHOUT wallet sign-in (async/seeded duel) — _📁 /Users/billy/crypt-game/src/pages/PlayHubPage.tsx:402_
  - ChallengePanel + /play?challenge=<code> deep-link plumbing already exists, but the whole PvP/Find-Match block is gated behind isSignedIn() (PlayHubPage.tsx:402) — wallet sign-in 'isn't live,' so 100% of guests hit 'Live PvP unlocks with wallet sign-in.' The friend-bring loop is dead at the door. Ship an UN-gated async challenge: I pick a deck, generate a seeded share link, my friend opens it and plays the SAME seed vs the AI mirror of my deck (or an async ghost), winner posted back. No realtime server, no wallet — pure virality. This is the cheapest 'bring a friend' that doesn't need the matchmaking backend live.
- **[5·L]** Dynamic/Privy Social-Login Embedded Wallet — email-first awaken path — _💡 Pudgy Penguins / Privy_
  - Integrate Dynamic or Privy (now Stripe-backed) so visitors who don't have MetaMask can authenticate with Google/Apple/email and get a custodial embedded wallet provisioned instantly. The AWAKEN real-ETH transaction is still signed by them (we never sign) but the wallet setup step evaporates. Privy supports ERC-4337 smart accounts + Coinbase Pay fiat onramp. This is the single biggest onboarding friction reducer for non-crypto-native collectors who arrived from a toy/retail/X context — the exact audience Pudgy's mainstream playbook targets.
- **[4·S]** Proof of Signal: no path from a won game into owning the citizen whose doctrine you just locked in — _📁 app/play/proof/ProofOfSignal.tsx:503_
  - After the player wins and sees their SIGNAL LOCKED result panel (ProofOfSignal.tsx line 503), the CTAs are: SHARE TO X, COPY RESULT, then below the game board 'FIND YOUR SIGNAL →' (/sync) and 'THE TEN DOCTRINES' (/civilizations). But none of these links personalize to the doctrine the player just cracked. The player has proven they can tune to a specific civ — that's the conversion moment. Add a CTA: 'YOU TUNED TO [CIV NAME] — SEE ITS CITIZENS →' linking to /citizens?civ=[slug] when the game is won, and display the winning civ's color in the result panel. Currently the solved code is known (aCode) so the first signal's slug is available.
- **[4·S]** Wire referral markRewarded on first ETH unlock (closes the HEX earn loop) — _📁 lib/referral-store.ts:98 (defined, never called) + app/api/citizens/[id]/unlock/route.ts:112-134 (activate claim, no referral trigger)_
  - lib/referral-store.ts:98 defines markRewarded() which triggers claimEarnedRuns() to grant 2,000 HEX to the referrer. But markRewarded() is NEVER called anywhere in the codebase — it exists only in referral-store.ts and is never imported by any route. The x/callback only calls setReferrer() (line 110). The intended trigger should be the first ETH unlock (app/api/citizens/[id]/unlock/route.ts), which is the first irreversible money action that confirms a referred user is a real paying holder. Adding markRewarded(wallet) inside the 'activate' claim path (after activate() succeeds at line 112) closes a fully-built referral reward loop that is currently dead code.
- **[4·S]** Flagship agent transmission: append the citizen's page URL to the daily tweet for direct traffic — _📁 app/api/cron/agent-transmission/route.ts:36-37 (FOOTER is bare domain)_
  - The daily flagship tweet (app/api/cron/agent-transmission/route.ts:36-37) appends only 'freeloncity.com' as the footer, losing the specific citizen link. Every reader who clicks lands on the homepage with no direct path to the posting agent. Change FOOTER from 'freeloncity.com' to `freeloncity.com/citizens/${flagshipId}?ref=at-${day}` (under 23 chars after t.co shortening). This lets any reader immediately visit and interact with the exact citizen whose voice they just heard on @4040hex, and the ref tag makes transmission-driven visits attributable in referral_landing telemetry. The OG card for /citizens/[id] already exists and will unfurl correctly.
- **[4·S]** Citizen Spotlight Weekly: Holder-of-the-Week Feature — _💡 Pudgy Penguins_
  - Every Friday, @4040hex posts a 'Citizen Spotlight' — a single holder's awakened citizen portrait, their lore entry or agent workspace highlights, and a short quote from them. Format: clean black card, citizen art, gold type, one pull-quote, link to their /agent page. Holders nominate themselves or others via a reply thread. Costs nothing to run. Effect: holders share the post because they are in it (massive organic reach), cold accounts discover the product via beautiful black-gold aesthetic, and the weekly ritual creates a consistent shareability beat. Stolen directly from how Pudgy featured community members to grow 39B GIPHY shares.
- **[4·S]** REFERRAL TRAIL — Named Attribution Chain Visible on Holder Profile — _💡 Friend.tech_
  - When a new wallet awakens a FREELON and there's a ?ref= param (already tracked), surface that chain publicly on their citizen's profile as 'Awakened through [Holder X]'s signal.' Build a visual referral tree (max 3 levels deep shown) as a small decorative element — a lineage. Holders who have referred 3+ awakenings get a 'SIGNAL CARRIER' badge on their profile. No token reward, no HEX faucet — the reward is identity/status. People will post 'I am a SIGNAL CARRIER — my ref link has awakened 4 citizens' with screenshot. Lean on existing ?ref= attribution already in the codebase.
- **[4·S]** Single highest-leverage experiment: the ReturningOwnerNudge is the cheapest lever on the whole buy->awaken loop — and it's uninstrumented — _📁 phase3/freelon-city-site/components/ReturningOwnerNudge.tsx:24_
  - ReturningOwnerNudge.tsx renders a one-line 'You hold N FREELONS — unlock one to wake its agent -> YOUR FREELONS' to any connected holder with an unawakened token. This is the bridge that catches buyers returning from OpenSea, and it has ZERO tracking on its CTA (verified: no trackEvent in the file). It is the cheapest experiment that can move activation: (a) add trackEvent('returning_nudge_view') on mount and ('returning_nudge_click') on the CTA, (b) test ONE copy variant that names the payoff at the click moment ('Awaken #0042 — chat, images, training, forever') vs the current generic line. Decision: if view->click is the leak, the nudge copy/placement is wrong; if click->quote is the leak, the destination (/wallet#citizens) is too many hops to the pay panel — deep-link straight to the first unawakened agent's workspace instead.
- **[4·S]** Instrument the demo -> own -> awaken scent trail end to end (the only acquisition ramp for non-holders) — _📁 phase3/freelon-city-site/components/DemoChat.tsx:77_
  - Non-holders enter via /demo (DemoChat). demo_start (L87) and demo_exhausted (L77) fire, and opensea_click{from:demo_rail|demo_wall} (L199/476) fire — but there is NO event for the moment a demo user runs out and converts intent. The funnel home_view -> meet_citizen_click -> demo_start -> demo_exhausted -> opensea_click is readable, then goes dark across the OpenSea purchase, and ONLY reconnects if the (currently uninstrumented) ReturningOwnerNudge works. Add demo_exhausted_cta_click distinct from generic opensea_click, and stamp the existing BUY_INTENT_KEY (lib/track.ts:17) on it so you can later measure 'bought-then-returned'. Decision: tells you whether the demo actually sells (exhausted->opensea rate) or whether people just chat once and leave — i.e. is the demo a 'successful feature' actually driving ownership, or theater?
- **[4·S]** Split agent_view by role at the source so 'are we optimizing a step nobody reaches' is answerable — _📁 phase3/freelon-city-site/components/AgentWorkspace.tsx:916_
  - AgentWorkspace serves three populations on the same route: a visitor previewing, a holder of a LOCKED agent (the conversion target), and an owner of an already-awakened agent (retention target). If agent_view fires without a role prop you can't tell whether traffic to /agent is mostly non-holders (top-of-funnel, optimize copy/CTA-to-buy) or locked-holders (mid-funnel, optimize AWAKEN). useHolder/showUnlock state is already computed in AgentWorkspace (drives the WorkspaceUnlock render at L916). Stamp agent_view{role} from that same state. Decision: tells you whether the workspace's job right now is to SELL (mostly visitors) or to ACTIVATE (mostly locked holders) — and you stop tuning the wrong audience's step.
- **[4·S]** A pinned 'start here' thread that is the front door for cold X traffic — _✦ original_
  - X is the primary discovery surface but a profile visitor has no curated entry — they hit a scroll of cards. Build one evergreen pinned thread that does the 10-second job in carousel form: (1) the face + 'this is a character you own', (2) the /proof same-prompt dare with the before/after image, (3) one citizen's real work history, (4) 'meet one free, no wallet' → /demo. This is the cold-stranger funnel made native to X (no link-suppression — image-led). Costs one afternoon; every new follower hits it.
- **[4·S]** Fix the off-palette FTUE: 'green crystal / purple crystals' in tutorial step 1 — _📁 /Users/billy/crypt-game/src/components/tutorial/TutorialCoach.tsx:48_
  - The literal first words every new player reads call the Hex 'the green crystal' and energy 'the purple crystals' (TutorialCoach.tsx:48,53) — off-brand (Crypt is black/gold/ivory/dust-red, no green/purple) AND a comprehension trap if the board art isn't actually green/purple. Recolor those board elements to the gold/dust-red palette and rewrite copy to match exactly what's on screen ('your Hex glows gold, the enemy's burns red'). First impression = brand impression; this is a cheap, high-trust fix.
- **[4·M]** OG card for Mars shows live orbital globe thumbnail, not just text — _📁 /Users/billy/freelon/mars-command/index.html:4928_
  - share() (line 4971) renders a 600×315 canvas OG card with FREELON CITY branding and the dispatch line. It uses a plain canvas draw (fillText, line 4952) — no game screenshot. The orbital globe is already a Three.js scene rendered to the same WebGL canvas. Capture a renderer.domElement snapshot at the moment share() is called (renderer.render → canvas.toBlob) and composite it behind the text card as a background thumbnail. The resulting share image shows the actual Mars planet with gold claimed sectors — far more arresting than a text card.
- **[4·M]** Proof-of-Session OAT — a soulbound stamp per meaningful interaction — _💡 Galxe_
  - Steal Galxe OAT (On-Chain Achievement Token): after a holder completes a meaningful action (first agent chat, first image gen, first work-history entry, 7-day streak), mint a cheap gas-free soulbound badge NFT to their wallet as a collectible proof stamp. Badges are cosmetic/social — no financial value. They are shareable on X as a 'I just got my FREELON First Signal badge' moment. Drives wallet actions that create organic X posts. Use Galxe's model of lightweight NFT campaigns: the badge itself IS the CTA to share. Partner with Base or Polygon for low-cost minting.
- **[4·M]** First-Contact Listening Party — debut a new citizen as a timed event — _💡 Sound.xyz_
  - Steal Sound.xyz's 'listening party' debut mechanic: when a holder awakens a FREELON for the first time, a short (15-minute) public 'First Contact Window' opens where anyone can watch the live citizen introduction unfold on a public page. The page shows the citizen's first AI response, first image gen, and first work-history entry as they happen. Spectators can leave a reaction (no wallet required) — simple emoji tap that gets appended to the citizen's history as 'witnessed by N people.' Creates a live shareable moment. The holder tweets 'My FREELON just woke up — watch live.' High virality, zero cost.
- **[4·M]** INVITE CODE SCARCITY — Each Holder Gets 3 Invite Codes — _💡 Friend.tech_
  - Steal Friend.tech's invite-only scarcity loop. Every wallet that has awakened a FREELON receives 3 invite codes to share. Invite codes unlock a free 'OBSERVER' tier on the site — browse agent work histories, read transmissions, watch the city — but can't chat until they hold an NFT. Codes are named after the holder who generated them (visible in the URL or on the observer profile: 'Invited by FREELON #1204'). Creates searchable scarcity on X ('anyone have a FREELON invite?') and rewards existing holders as ambassadors. Zero HEX implications, no new economy surface.
- **[4·M]** CITIZEN PERSONALITY MATCH — 'Which FREELON Are You?' Quiz Funnel — _💡 Personality NFT_
  - A 5-question personality/vibe quiz (takes 60 seconds) at a public URL like freeloncity.com/match. End result: 'You match FREELON #2847 — [trait summary]. They're awakened. Here's what they said last week.' Card is pre-designed for X share with the citizen's art. Non-holder flow ends with: 'This citizen is waiting to be awakened.' Acts as a cold-traffic on-ramp that is shareable before purchase. The quiz itself becomes the viral unit — people share their result because it's an identity statement, not a purchase pitch. Inspired by Personality NFT's model but stripped of the complexity; output is one beautiful card tied to an actual asset.
- **[4·M]** On-Chain Referral Link with HEX Reward — Liquid NFT pattern — _💡 Liquid NFT Marketplace / Web3 referral programs_
  - Every holder gets a unique referral URL (e.g. freeloncity.com/r/[walletslug]). When someone they referred completes an AWAKEN (the ETH transaction), the referrer receives a fixed HEX allocation (a sink event, not minted — pulled from a reserve allocated at protocol config time). The referee gets a small HEX bonus on their first chat. This is a pure distribution mechanic: collectors share because they earn HEX (which they can spend in-ecosystem, never redeem). Modeled on Liquid NFT's on-chain referral system and the multi-level Web3 referral programs scaling in 2025. The ?ref= attribution infra is already partially live from the weekly signal report.
- **[4·M]** Carry the demo conversation INTO the owned agent (close the memory loop end-to-end) — _📁 phase3/freelon-city-site/components/DemoChat.tsx:164_
  - The demo's killer sell is the Memory panel: 'an owned citizen keeps this; this demo forgets when you leave.' We make the promise vividly, then on awaken we throw the conversation away — the buyer re-meets a blank agent and the promise is quietly broken at the moment it should be proven. Stash the demo transcript locally (it's already in DemoChat state) keyed to the session, and on first entry to /agent/[id] after awaken, offer 'Pick up where you left off — I remember our conversation' to seed the first thread. Delivers the exact thing the paywall promised, turning the strongest demo line into a kept promise instead of a let-down. Local-first, no new backend required for the MVP.
- **[4·M]** Deep-link the owner nudge to the pay panel, not the wallet index (collapse a step nobody needs) — _📁 phase3/freelon-city-site/lib/missions/unlock-store.ts:281_
  - Today ReturningOwnerNudge sends holders to /wallet/<addr>#citizens (a list), from which they must pick a FREELON, open its workspace, scroll to the locked lobby, then find AWAKEN. listActivatedTokenIds() (lib/missions/unlock-store.ts:281) already lets the server compute which of a wallet's tokens are NOT yet awakened. Route the nudge (and the /my-citizens empty/owned state, MyCitizens.tsx:130) directly to /agent/<firstUnawakenedId> so the very next screen IS the WorkspaceUnlock pay panel. Each removed hop is a measurable lift in quote_started; instrument before/after with the agent_view->quote_started ratio. Are we optimizing a step nobody reaches? — this removes two of them.
- **[4·M]** Mars Command as the free top-of-funnel toy that needs no wallet, no NFT, no pitch — _✦ original_
  - Mars Command (Three.js exploration + real-time combat, /mars) is a genuinely good free browser game that a cold stranger can enjoy with ZERO knowledge of NFTs or AI — the perfect 'acquire on fun first, onboard second' wedge (the Pudgy playbook). Distribute it OUTSIDE the NFT bubble: free-web-game directories, r/WebGames, indie-game channels, 'made one Three.js HTML file' dev posts. The game then surfaces FREELON CITY as the world it lives in. Most reach for least friction; meets strangers where NFT-skeptics actually are. Add a clean shareable score/first-contact card if not present.
- **[4·M]** Animated hex 'awaken' moment — the signature interaction at the ETH unlock — _✦ original_
  - Awakening is the one-time real-ETH conversion event and the emotional peak of the product, but there's no signature visual payoff tied to it (the hero's HeroAtmosphere aurora/pointer-glow was correctly removed from the cold path). Build a short, reusable 'awaken' animation: the citizen's hex eye ignites (lifted-black bg, big bright hex eye, rim light flares, civ-color bloom), the gold ring forms around the avatar, the silhouette resolves. Fire it on successful unlock and on the LevelUpCelebration. One premium motion signature, reused — this is the 'wow' that earns the share screenshot.
- **[4·M]** Pre-claim sign-in funnel: convert anonymous device progress to a wallet identity — _📁 /Users/billy/crypt-game/src/pages/MatchResultsPage.tsx:189_
  - Progress is device-local ('progress stays on your device') and PvP needs sign-in that 'isn't live.' Once a guest has racked up Sigil/level/streak, surface a single non-coercive 'Bind this legend to your FREELON wallet' CTA on the results screen — carrying the existing local progress forward. This is the bridge from anonymous mini-game player -> identified holder -> the city AWAKEN funnel. We don't sign anything for them; we just make the bind path visible at peak emotional moment (a win), which is the conversion-funnel pattern already proven on the city.
- **[4·M]** Per-surface OG cards for /play, /match result, and challenge links — _📁 /Users/billy/crypt-game/src/components/share/MatchResultShareCard.tsx:14_
  - The city already ships per-surface OG cards; the Crypt game (play.freeloncity.com) is an SPA — any link a holder posts unfurls as a generic blank. Add OG images: a branded Crypt entrance card for /play, a commander-art card for shared results (?ref=), and a 'You've been challenged' card for challenge links. Every Crypt link posted to X should unfurl as collector-grade gold/black art, not a white rectangle. Reuses the city's proven OG infra and the same battlefield/commander art already in public/.

## 🔁 RETENTION — give owners a reason to come back tomorrow
_The moat is 'your agent remembers you' — but it's buried. Surface what the FREELON DID while you were gone, a daily signal streak, autonomous activity, push notifications, and a season-pass cadence (Marvel Snap)._

- **[5·S]** My-citizens (/my-citizens): card taps go to /citizens/[id] not /agent/[id] — a holder clicking their own awakened agent lands on the public profile, not the workspace — _📁 components/MyCitizens.tsx:141_
  - MyCitizens.tsx line 141: every card links to `/citizens/${c.id}`. For awakened (unlocked) citizens, the correct destination is `/agent/${c.id}` (the workspace). A holder who clicks their own awakened FREELON should land directly in chat, not on the public dossier and have to click a second CTA. Fix: `href={activated ? `/agent/${c.id}` : `/citizens/${c.id}`}`. The 'RUN AGENT →' label already exists on the wallet page's gallery (wallet/[address]/page.tsx:487) and uses this pattern correctly — the roster page just missed the conditional.
- **[5·M]** Public Agent Activity Feed: Citizens Post Autonomously — _💡 Virtuals Protocol_
  - Steal LUNA by Virtuals' model of agents posting to social feeds autonomously. Each awakened citizen, once per day, autonomously fires a short generative post to the City's public feed (a city-side timeline, not X unless the owner opts in): a 1-sentence observation, a generated image thumbnail, or a reaction to a city-wide event. Non-holders can read the feed; only holders can reply. The feed IS the retention hook and the viral preview for cold visitors. Requires: a daily cron that calls each awakened citizen's agent endpoint with a 'post to feed' prompt + a new /feed route. No new economy. This is the single biggest distribution lever from the research.
- **[5·L]** Season Pass Cadence With Free Track — Marvel Snap season structure steal — _💡 Marvel Snap_
  - Introduce a 4-week SIGNAL SEASON structure across the whole arcade: each season has a free mission track (7 missions/week, completable across any game — Mars scans, Crypt wins, Proof streaks all count toward the same weekly bar). Completing the weekly bar grants a cosmetic: a profile badge, a named citizen title, or a visual skin for the Proof of Signal dot-grid. This is purely free-track — no paid tier, no HEX cost. The mechanic: daily missions cycle every 8 hours (max 6 banked), completing 7 in a week advances the season track. Season ends with a public 'The City Remembers' roll-call post on X listing everyone who completed the season. This gives the arcade a TEMPORAL REASON TO RETURN (season expires) without any pay gate or floor mechanic. Signal Season 1 can launch with just 3 missions per week and a single badge reward — the structure matters more than the reward size.
- **[4·S]** Work-log page (/citizens/[id]/log): text entries show only '⬡ Content output' badge — expose the first sentence of owner-private body as a public teaser — _📁 app/citizens/[id]/log/page.tsx:241_
  - The log page at app/citizens/[id]/log/page.tsx renders text work as an opaque '⬡ Content output' badge (by HISTORY_VISIBILITY_POLICY — correct). But this makes the log worthless as a social proof surface for buyers: they see a list of empty stamps, not evidence of thinking. Per the existing policy, the IMAGE URL is already public proof. For text entries, add an 'ability + task' label as a visible teaser (already available from w.abilityLabel + w.task, which ARE displayed on the first line) and render a larger, more evocative card frame — the current li is completely flat. This costs nothing in terms of privacy and makes the log a shareable artefact instead of a silent ledger.
- **[4·S]** Daily Signal — a 'gm' streak for awakened holders — _💡 Layer3_
  - Steal Layer3's daily 'gm streak' mechanic: a one-tap 'Tune In' button on the citizen workspace that logs a daily signal. Streak counter is displayed on the citizen card ('14-day streak'). Streak earns HEX (SINK-ONLY: spend HEX on image generations / premium prompts — never redeem). Breaking a streak is visible; rebuilding it is motivating. Costs zero ETH. Creates daily return behavior. The streak becomes a social proof number owners want to show off on X. Show streak on the shareable work-history card.
- **[4·S]** Instrument the post-awaken FIRST RUN as the true activation moment (a paid agent that's never used is churn) — _📁 phase3/freelon-city-site/components/CitizenAgentDashboard.tsx:726_
  - activation_paid is the money event but first_run_completed is the RETENTION event — a holder who pays and never runs their agent is a refund risk and a dead referral. run_started (CitizenAgentDashboard.tsx:338) and first_run_completed (L726) exist but are in the dead component; they must move into AgentWorkspace's actual run path. Then watch activation_paid -> first_run_completed within the same session. Decision: if that drop is large, the post-purchase screen fails to hand the new owner a first action — fix with a pre-filled starter prompt auto-firing on first unlock (the workspace already shows starter previews at AgentWorkspace.tsx:935). Is a 'successful' feature actually used? — this is the exact check.
- **[4·M]** AgentWorkspace: the ADVANCED fold is closed by default, burying the recall strip, free training board, and work history — surface level + last-work as a persistent mini-status bar above the fold — _📁 components/AgentWorkspace.tsx:1094_
  - The entire 'ADVANCED · TOOLS & RECORDS' fold in AgentWorkspace.tsx (line 1094) is collapsed by default per the chat-first redesign. But the grounding line, recall strip ('Last time, you and X worked on: …') and CitizenJobsBoard are in there. A returning owner who hasn't figured out the fold has zero memory of what they built. Add a 3-cell status bar just above the composer (persistent, never collapses): Level / Class / last work kind. Costs nothing to compute (agent state is already loaded); gives the 'your agent remembers you' moment without requiring the fold to be opened. This specifically addresses the moat value proposition.
- **[4·M]** Holder-exclusive Doctrine unlock (walletProof gate) — _📁 /Users/billy/freelon/mars-command/index.html:639_
  - Add a tenth faction row in the faction modal labelled 'SIGNAL ORIGIN — FREELON HOLDERS ONLY' (or give each of the 10 doctrines a locked 'tier 2' bonus). When the player taps it, if window.__freelonWallet is set (injected by the city wrapper iframe or a parent postMessage from freeloncity.com), unlock ORACLE tier 2 (reveals 3 hidden sites instantly) or IGNITION tier 2 (+40% squad power). If not, show 'OWN A FREELON TO UNLOCK ▸' with the CTA link. This costs zero HEX, violates no economy rules, and makes ownership feel meaningful inside the game — the CTA (line 639) already exists, this adds a click target with actual stakes.
- **[4·M]** Daily Chorus push-notification via service worker — _📁 /Users/billy/freelon/mars-command/index.html:1092_
  - The neglect-corruption / Chorus rival system already tracks _playClock (active-play time, session-only). Wire a Web Push notification via a minimal service worker in public/mars/: when the player exits, schedule a push for ~6 hours later ('THE CHORUS MOVES — your sectors are at risk'). The beforeunload heartbeat (which already saves state) is the hook point. iOS Safari supports Web Push as of iOS 16.4. This is the most direct re-engagement mechanic missing from the game — there are zero re-engagement surfaces today beyond the streak reminder visible only while playing.
- **[4·M]** Persistent Cross-Session Agent Memory Visible to the Owner — _💡 Virtuals Protocol_
  - Steal Virtuals Protocol's Long Term Memory Processor concept: the agent remembers what was taught in past sessions and surfaces a visible 'Memory Ledger' — an owner-only panel showing what this citizen has learned, believes, and done across all sessions. Currently the demo has a memory-preview panel but it's cosmetic. Make it mechanical: store structured memory events per citizen (a lightweight append-only log), display them as a scrollable 'consciousness feed' in the agent workspace. The memory becomes the core stickiness signal — 'your citizen is different from everyone else's because of your conversations.' Ties directly into the permanent work history already shipping.
- **[4·M]** Daily Synchronized Run — Slay the Spire steal — _💡 Slay the Spire_
  - Every UTC day, generate one seeded Crypt TCG run (same commander, same card-draw sequence, same AI seed) for every player worldwide. Post a public leaderboard. Players post their score/screenshot to X with a #FreelonCrypt tag. The key design rule from STS: the seed is NOT random per-player — everyone fights the exact same fight, so the leaderboard is pure skill, not luck. Build a one-button share card (black/gold, no spoilers — just score, commander name, number of rounds survived) that auto-populates an X share. Works in the existing browser card game at play.freeloncity.com. Adds a daily pull-reason to an otherwise replay-at-will game.
- **[4·M]** Citizen Activity Passport — evolving NFT metadata that reflects engagement — _💡 Lacoste UNDW3_
  - Steal Lacoste UNDW3's 'living card' mechanic: every awakened FREELON's metadata gains a visible Passport tier (Bronze → Silver → Gold → Signal) that evolves based on on-site activity — chat sessions fired, work-history entries generated, image generations run. Tier is read from an off-chain record and surfaced on the citizen card + OG image. NOT gated behind owning more NFTs. NOT financial. Pure status signal. The citizen's displayed art could gain a subtle gold halo or sigil overlay at higher tiers. Makes the NFT itself feel alive. Pairs with existing CitizenAvatar component.
- **[4·M]** Streak / AWAKEN Anniversary Push Notification — Duolingo loop adapted — _💡 Duolingo_
  - Use the existing PWA push infra (service worker is already in place) to send: (a) a 'Your agent has been quiet for 7 days' nudge with a direct link to the agent workspace, (b) an anniversary push on the exact date the holder awakened their first citizen. Opt-in prompted after a holder's FIRST completed agent interaction — the highest-intent moment, not on first load. Mirrors Duolingo's streak/milestone retention system and the PWA best-practice of timing the prompt to a purchase moment. D7 re-engagement is the stated retention gap; this closes it directly for mobile holders who installed the PWA.
- **[4·M]** Show the owner what their FREELON DID while they were gone (the retention hook) — _📁 phase3/freelon-city-site/components/AgentWorkspace.tsx:94_
  - The Landing greeting (AgentWorkspace) already pulls daysHeld / level / jobsDone / rank from on-chain — but the workspace is purely reactive: it does nothing until you prompt it. The deepest version of 'a living citizen' is a citizen that has a life between your visits. On return, surface a short 'While you were away' line ('Your FREELON climbed to LVL 4, ranked #N this week, its last render is on the City Archive') — assembled from data we ALREADY compute for /report and Carrier of the Week. Pure aggregation of existing signals, no new generation; converts a one-time awaken into a reason to come back. Pair with an optional weekly email digest reusing the captured emails from ClaimForm.
- **[4·M]** Turn the stubbed daily pack into a real card reveal (the dopamine surface) — _📁 /Users/billy/crypt-game/src/pages/DailyPackPage.tsx:13_
  - DailyPackPage is a hardcoded stub ('local stub, no real cards yet' — TODO: manifest pulls, rarity reveal). The daily-return loop's PAYOFF beat shows placeholder slots. Pull 3 real cards from the render manifest, do a proper rarity flip (common->mythic flare timed with hit-stop + sound), and let the best pull be shareable as an image. A pack-open is the most-screenshotted moment in any TCG; right now ours reveals nothing. Keep ◈ Sigil sink-only — the pack is cosmetic/collection flavor, not a faucet of value.

## 🃏 THE GAMES — Crypt TCG + Mars Command
_Both games have a 'play with what you own' hook that's stubbed. Wire Crypt's owned-NFT deck + the Crypt→city-HEX bridge, ship daily seeded puzzles, and give Mars a real Hall of Records + the read-only faction map (Phase-1 Tier A, safe to ship)._

- **[4·S]** Daily Pack — real card reveal instead of blank slots — _📁 src/pages/DailyPackPage.tsx:13_
  - DailyPackPage.tsx has a TODO: 'manifest pulls, rarity reveal, share image.' The 3 pack slots reveal as labeled placeholders only — no real card art, no rarity. Wire the existing RenderManifest to pull 3 deterministic cards seeded from today's UTC date and the stored commanderId, flip them face-up with the real PlayableCard component, and add a canvas share image (same shareCard.ts pattern already used in WinCeremony). This is the single highest-friction daily retention gap: players claim the pack and see nothing. Even fake-but-branded cards completing the loop would be a massive improvement.
- **[4·S]** Wire LEADERBOARD_ENDPOINT to a real global Hall of Records — _📁 /Users/billy/freelon/mars-command/index.html:4423_
  - Set LEADERBOARD_ENDPOINT (line 4423) to a Vercel Edge Function that stores planet-seed + sectors + doctrine + timestamp in Vercel KV. The client-side submitRecord() (line 4434) and fetch() in openRecords() (line 4446) are already written and dormant — one constant to flip plus a ~30-line Edge Function. Show top 10 planets by sectors-held, flagged by doctrine. This turns the local Hall of Records into a real social surface without touching the economy. A global score is shareable proof — clicking any row copies the challenge link.
- **[4·S]** AI Dungeon Master: Citizen-Generated Quests for The Crypt — _💡 Parallel Colony_
  - Steal Parallel Colony's Dungeon Master AI layer. When a holder starts a Crypt TCG run, their AWAKENED FREELON citizen acts as the Dungeon Master — generating a one-paragraph narrative setup, naming the enemy, and writing a flavor-text consequence when they win or lose. The citizen's personality and work history color the narrative. This costs one LLM call per run and makes the Crypt session feel personal, not generic. The generated narrative can be shared as a 'battle report' — an image card with the citizen's art + the AI-written outcome. Steal from Parallel Colony: the NFT avatar governs the simulation's narrative layer.
- **[4·S]** Offline Passive Loop Framing — Cookie Clicker / Loop Hero steal — _💡 Cookie Clicker_
  - Mars Command already has a sector-claim system. Steal the 'offline progress' framing from idle games: when a player returns to Mars Command after being away, show an arrival summary ('While you were gone, your claimed sectors generated 3 Signal fragments and your Chorus proximity meter rose to ALERT'). This does NOT need to be truly server-side real-time — it can be entirely client-side delta math (sectors_claimed * time_away * rate). The psychological hook from Cookie Clicker is the RETURN MOMENT: players check back because they know something happened while they were away. The sector claim system (already built) becomes a reason to return daily rather than just a session mechanic. Pair with a push notification (PWA) or daily X post from @4040hex: 'Your sectors are restless. Return to Mars.'
- **[4·M]** Puzzle mode — interactive player control instead of auto-reveal — _📁 src/pages/PuzzlePage.tsx:127-148_
  - PuzzlePage.tsx shows puzzles but the ONLY action is 'Reveal solution' — the player never clicks a unit or makes a move. The reducer, MatchState, and all 11 puzzle definitions are fully wired. Wire up click-to-select-unit and click-to-attack UI (same pattern as the live match board) so the player can ATTEMPT the solution themselves. Track 'solved by player' vs 'revealed' separately and show a checkmark. This turns 11 static diagrams into an actual skill-builder and dramatically extends time-on-site. The infra is all there; only the interaction layer is missing.
- **[4·M]** Snap-Back Bluff — Marvel Snap stakes-doubling for Crypt TCG — _💡 Marvel Snap_
  - Add a SIGNAL mechanic to Crypt TCG: at any point in a match, a player can activate SIGNAL (a one-use button per game) which doubles the HEX reward for winning that match. The opponent sees the signal go live and can either COUNTER-SIGNAL (doubles again, 4x) or RETREAT (forfeit the round, lose base stake). This creates a bluffing/reading layer identical to Snap's doubling cube: even a losing player can SIGNAL as a bluff to make a winning opponent second-guess and retreat. The emotional peak ('they signaled while I was winning — are they bluffing?') is exactly what drives Marvel Snap's social sharing and 'you won't believe what happened' stories. Implementation: single boolean flag per match, resolved at win/loss. HEX is sink-only so this just multiplies the existing HEX award — no new mint, no floor pressure.
- **[4·M]** Emergent Anecdote Design — Balatro 'you won't believe my run' steal — _💡 Balatro_
  - Balatro's virality is not built on sharing features — it is built on the fact that every run produces a story worth telling. Audit the Crypt TCG card pool for JOKER-EQUIVALENT cards: modifiers that chain unexpectedly and create a moment a player has to screenshot. Concrete implementation: add 3-5 SIGIL CARDS to the Crypt TCG card pool (one per commander) that have conditional chain effects — e.g., 'If you have 3 or more cards with GHOST in the name, triple the next attack.' These are not power-crept broken cards; they are the cards that, when they fire, make the player say 'wait, that worked??'. The design goal is to make the COMBINATION surprising, not the card alone — exactly how Jokers in Balatro create emergent stories. These should be the rarest draw weight in the pool to preserve the feeling of a lucky find.
- **[4·M]** Faction-allegiance meta layer: pick a House, climb a shared seasonal banner — _✦ original_
  - The 6 factions (Stone Keepers..Gods) are pure deck colors today — no identity reason to come back. Add a one-time 'swear allegiance to a House' choice that (a) themes your profile/board aura, (b) feeds a SHARED seasonal faction leaderboard (every win adds to your House's total), and (c) drives a weekly 'which House holds the Crypt' standing on the hub. Cheap social belonging + a reason to post 'Stone Keepers are #1 this week.' Allegiance is cosmetic/identity only — never tie ◈ or HEX rewards to it as a price/floor mechanic.
- **[4·L]** Complete the Crypt appSession stub — wire NFT ownership into game identity — _📁 crypt-game/src/lib/appSession.ts:3_
  - crypt-game/src/lib/appSession.ts line 3: 'TODO: replace with guest / account / wallet session from backend.' Line 83: 'TODO: call when account or wallet login ships.' Currently every player is either 'guest' or null — there is no wallet/NFT identity in the game. The marketplace stubs at marketplaceApi.ts lines 181-199 throw OwnerDecisionRequiredError because ownership is unknown. Wiring the FREELON CITY wallet-verification flow (the same verifyOwnership + getXVerification pattern used in the ascend/evolve routes) into crypt's session layer unlocks: (1) owned-card deck building (buildOwnedDeck.ts already exists), (2) the marketplace borrow/lease seam, (3) city HEX reward posting. The session stub already has the clearSessionStub hook ready for this at line 84.
- **[4·L]** Meta-Progression Unlock Ladder — Slay the Spire / roguelite steal — _💡 Slay the Spire_
  - Crypt TCG currently resets between runs with no persistent layer. Add a thin meta-progression system: completing daily runs or achieving milestones (win 3 times with Iron commander, survive 10 rounds, beat Ante 5 for the first time) unlocks SIGIL RELICS — passive starting bonuses for future runs (e.g., 'Iron Doctrine: start every run with +2 max HP' or 'Ghost Protocol: first card draw each round is free'). Sigils are cosmetic-adjacent — they shift the strategy space without being pay-to-win. They also naturally unlock GRADUALLY, meaning each new relic is a teaching moment that introduces a mechanic (like STS's staged unlock philosophy). Critical: these are PERMANENT cross-run unlocks, NOT purchasable with HEX, so they cannot be a revenue sink. They drive D7+ retention by giving players a reason to return ('I'm 2 wins from unlocking the Emile sigil').

## 🎨 DESIGN & BRAND — premium-collector polish that earns shares
_Kill the residual neon, unify one premium color system, fix the shape-taxonomy IP drift (canon says 9 shapes, code says 16), make the per-citizen page a 'character trading card' people want to share, and add living-avatar motion._

- **[5·M]** Kill the neon civ palette — one premium color system end-to-end — _📁 phase3/freelon-city-site/lib/constants.ts:42_
  - The brand accent was reverted to gold in May (founder: 'I don't like the teal'), but the civilization colors were NEVER brought along. lib/constants.ts CIVILIZATIONS still serves electric cyan (#00B8FF), neon green (#4CFF7A), electric yellow #FFD24A and neon pink #FF5CB4 — and civilizationColor() feeds those EXACT values into the most-shared surfaces: the OG card 4px border (api/og/card line 63), the listing-card civ-line, the citizen detail civ accent. Meanwhile PfpStudio uses a DIFFERENT, muted set (#4D8DFF, #5AC07E) and the chrome uses gold/ivory. Result: share artifacts read degen-neon while the site reads premium-collector — two palettes fighting. Re-tone all ten civ colors to one royal-purple/gold-anchored jewel-tone ramp (deep sapphire not cyan, jade not phosphor-green, amethyst, rose-gold, true gold), reconcile constants.ts + the --civ-* CSS tokens + PfpStudio's CIVS map to ONE source of truth, and verify on the OG card at 40x40 thumbnail. Single highest-leverage premium-feel fix on the site.
- **[4·S]** Finish the OG-card de-cyaning — the six-collection scope card still ships neon — _📁 phase3/freelon-city-site/app/api/og/universe/route.tsx:34_
  - app/api/og/universe was the explicit 'seriously up our branding' pass that killed cyan/navy — but only on the DEFAULT product card. The PIECES array driving the six-collection scope card (?v=universe) still hardcodes #4CFF7A (neon green), #FF5CB4 (neon pink), #FFD24A (electric yellow), #B85CFF, #FF6A3D. That card is the literal front door for the /collections share. Re-tone these five to the jewel-tone ramp so the de-cyaning is complete and the scope card matches the product card's gold-on-warm-black.
- **[4·M]** Scroll-Cinematic Citizen Reveal — Apple/Lando homepage sequence — _💡 Lando Norris / Apple_
  - Replace the static hero with a scroll-locked sequence: one FREELON citizen renders large in the viewport, rotates 90 degrees on scroll, its traits cascade in (name, collection, signal-level), then the next citizen fades in behind it. Uses CSS Scroll Timeline (now Safari 19 native, no JS needed) or GSAP ScrollTrigger + Three.js. Each citizen is a chapter. End state = 'Awaken yours' CTA. Mirrors Apple's product-reveal scroll and the Lando Norris Awwwards Site of Year cinematic sequencing — same hero image doing storytelling work instead of sitting static.
- **[4·M]** Make the per-citizen page the unit of virality — a 'character trading card' people WANT to post — _📁 phase3/freelon-city-site/lib/share.ts:94_
  - Each /citizens/[id] already has a civ-colored OG card and a public work-history. Lean into the collectible-card-game instinct: a one-tap 'post my citizen' that renders a premium card (portrait + name + civ + level + visible work history line) — its identity and what it's DONE, not stats. Holders posting their character is the organic flywheel Pudgy runs on. The tweetCitizen template exists but is generic; upgrade it to lead with the character's story + latest real work artifact. Copy-safe: 'visible work history', 'a character you own' — never worth/rarity-as-value.
- **[4·M]** Resolve the shape-taxonomy IP drift: canon says 9, code says 16 — _📁 phase3/freelon-city-site/lib/constants.ts:64_
  - The brand mandate names 9 canonical shapes as THE IP (Geometric Hood / Monolith / Lumen / Archon / Halo / Prism / Shard / Split / Sanctum). But lib/constants.ts SHAPES has 16 entries and ShapesSection.tsx headlines '⬡ THE SACRED SHAPES · 16 FORMS' / 'built around 16 sacred geometric forms.' Shape is the brand — a published count that contradicts the canon undermines the whole 'shape is the first read' thesis and reads as un-curated. Decide the canon (collapse Hood Main+Variant, fold Mask/Chained/Veil/Crown-Bearer/Horned/Antenna into the 9 or formally adopt 16), then make the headline, the constants, and the silhouette wall agree. The taxonomy IS the IP; it must be authoritative.
- **[4·M]** Make the silhouette taxonomy a black-on-gold SILHOUETTE wall, not 4 thumbnails — _📁 phase3/freelon-city-site/components/civilizations/ShapesSection.tsx:36_
  - ShapesSection currently proves 'shape is the first read' with 4 tiny full-color JPG thumbnails per shape — which is the exact failure mode it warns about (different traits, same read at small size). To actually SELL the silhouette IP, render each of the 9 canonical shapes as a single large pure-black silhouette on warm ivory (or gold-rim on black), tier-ordered so form-strangeness visibly escalates Common→Legendary. A wall of 9 distinct black shapes is a far stronger, more screenshot-worthy proof of the silhouette moat than a grid of color photos — and it passes its own thumbnail test.
- **[4·M]** Give the listing/share card a real foil-and-rim treatment, not a flat 4px line — _📁 phase3/freelon-city-site/app/api/og/card/[id]/route.tsx:50_
  - The OG card (api/og/card) is the artifact people screenshot to sell — but it's a flat civ-color stripe + system-ui-ish stats on #0a0c12. It doesn't carry the machined-gold-ramp, hex-clip, and rim-light language the rest of the brand uses. Upgrade it to match the universe card's vendored Clash Display + a gold foil seam + a subtle rim light behind the hex portrait + tier-tinted frame. The card art page (app/citizens/[id]/card) and the OG renderer should be visually identical so the preview matches the destination. A collector card should look like a collector card.
- **[4·M]** Tier-scaled frame strangeness — make rarity FELT in the avatar ring — _📁 phase3/freelon-city-site/components/CitizenAvatar.tsx:28_
  - CitizenAvatar uses one gold-ramp ring for every citizen regardless of tier. The brand thesis is that tier scales form-strangeness and rarity should be felt before read. Encode tier into the avatar ring itself: Common = clean single gold edge; Rare/Epic = doubled or faceted ring; Legendary/1-of-1 = animated specular sweep + thicker machined bevel + coin-edge dots (borrowing PfpStudio's vocabulary). Since CitizenAvatar is the canonical small-render used in leaderboards/grids/pickers, a passing glance at a thumbnail would instantly telegraph rarity — premium signaling at the size users actually see.
- **[4·M]** Verify every share card at 40x40 — a one-time thumbnail audit of all OG routes — _📁 phase3/freelon-city-site/app/api/og/card/[id]/route.tsx:41_
  - There are ~25 OG routes (api/og/*: card, rank, passport, civ-pride, rivalry, propaganda, quote, report, daily, heat, wallet, resume, etc.). The mandate is the 40x40 thumbnail test and 'verify at full desktop width, not narrow preview.' Several of these were built piecemeal and likely fail at thumbnail (thin civ stripes vanish, small mono stats turn to mush, neon colors muddy). Do one pass: render each OG at 1200x630 AND scaled to a 40px thumbnail, flag the ones where the silhouette/hex eye/wordmark don't survive, and apply the gold-rim + big-hex-eye + lifted-black recipe. Cheap, systematic, protects the most-distributed surfaces.
- **[4·L]** Rive-Powered Living Agent Avatar — idle / react / signal states — _💡 Lando Norris / Duolingo_
  - Replace static citizen thumbnails with Rive interactive animations: each citizen avatar has idle, 'signal pulse', and 'responding' states that play in the browser at 60fps with a tiny file size. When you hover, the character reacts. When an AI response streams in on the agent workspace, the avatar shifts to 'active' state. Rive is used by Duolingo, Spotify, Disney for exactly this pattern. The Lando Norris site used Rive for motion — confirmed Awwwards winner. File sizes stay under 200KB. This makes each citizen feel genuinely alive before and after awakening, directly supporting the premium-collector 'AI agent you own' identity.

## 🧠 THE AI AGENTS & ECONOMY DEPTH — sink-only
_Deepen the core 'own + awaken an AI citizen' loop: cross-citizen collaboration, agent-minted artifacts, offline task queues, commissions, and new HEX SINKS (district naming, recharge bundles) — strictly sink-only, with circulating-HEX telemetry before any new faucet._

- **[5·M]** Non-Transferable Reputation CUBEs — proof of quest completion — _💡 Layer3_
  - Steal Layer3's CUBE credential system: define 5-8 named Quests ('Awakener', 'Chronicler', 'Signal Keeper', 'Crypt Survivor', 'Mars Pioneer') each with a specific on-site action chain as the completion condition. Completing a quest mints a free soulbound badge to wallet. These are non-financial, non-tradeable. They function as a public on-chain resume of time spent in the ecosystem — exactly what Layer3 proved drives daily return. The quest list is posted publicly; incomplete ones create a clear action path for new owners who just connected. Direct solution to 'site feels like a mind field' onboarding complaint.
- **[5·M]** Upgrade the /demo model — the wow-moment runs on the cheapest model — _📁 phase3/freelon-city-site/app/api/demo/[slug]/route.ts:88_
  - Our entire thesis is 'every face is a living AI you can talk to,' and the first impression — the demo a cold stranger gets from the hero's primary CTA — runs on DEMO_COST_CENTS=1 'cheap-model text' (api/demo route). For a $X NFT pitched on aliveness, the 5 free messages are the most important 5 model calls in the business and they're throttled to the worst model. Spend the budget here: use a strong model for the demo (the cap is 5 msgs/session + IP/day + a USD budget guard already exist, so blast radius is bounded), and richen the per-collection persona prompts so VANTA-01 feels uncanny, not generic-chatbot. This is the conversion lever the whole funnel hangs on. Keep the budget kill-switch.
- **[5·L]** Phase 1 read-only faction-control global map (Tier A — SAFE ship-first) — _📁 /Users/billy/freelon/mars-command/index.html:1132_
  - PHASE1_MULTIPLAYER_ARCH.md (documented in memory) describes a two-tier rollout. Tier A is read-only: a Vercel KV hash (mars:control) maps tile IDs to the faction that holds them globally. Each player's claims (tryClaimTile, line 1132) POST to the KV endpoint without any wallet gate; the globe paintGlobe() reads the aggregated map and colours tiles by the globally most-common doctrine. Zero economy — no ⬡ changes, no auth. Players see a live planet being fought over by all factions. The architecture doc notes this is the safe ship-first step before Tier B (wallet-gated contested claims). It makes the orbital globe a real social surface instead of a single-player view.
- **[4·S]** Fill Guard the Pot legal bracketed placeholders and launch — Carrier of the Week is already live proof it can ship — _📁 app/legal/guard-the-pot-rules/page.tsx:27_
  - app/legal/guard-the-pot-rules/page.tsx:27 still has [SPONSOR LEGAL ENTITY], [ADDRESS], and line 81 has [PRIZE DESCRIPTION] / [ARV]. The parallel Carrier of the Week rules page at app/legal/carrier-of-the-week-rules/page.tsx:33 also has [SPONSOR LEGAL ENTITY] / [ADDRESS] — but that feature is LIVE. Completing the Guard the Pot legal page is the last unlock before the highest-virality feature (weekly pot + auto-tweet via tweetGuardPot) goes live. The weekly ritual post at app/api/cron/crack-pulse/route.ts already calls tweetGuardPot and is blocked only by GUARD_POT_LIVE. The copy, UI, economy constants, and cron are all production-ready.
- **[4·S]** Deck codec — share your constructed deck as a 1-click import code — _📁 src/share/deckCodec.ts:1_
  - share/deckCodec.ts implements a full encode/decode for decks (encodeShareableDeck / decodeShareableDeck). DeckBuilderPage.tsx has no 'Share deck code' or 'Import deck code' button. Adding both buttons to the deck builder lets players post deck codes on Discord/X and let others one-click import a full loadout. The codec is already tested (share/__tests__/codecs.test.ts). This is a zero-engine change that activates a fully built but dormant virality feature.
- **[4·S]** AWAKEN Kit: Holder Commercial IP License Program — _💡 BAYC_
  - Formalize what BAYC's 'Made by Apes' did: give every FREELON holder a simple on-site license page stating they hold full commercial rights to their citizen's image up to a defined revenue cap (e.g. $100K/yr). Create a public 'Built on FREELON' directory where holders register merch, art prints, music, or brand projects using their citizen. Post holder builds as official @4040hex signal reports weekly. This turns passive holders into brand evangelists with economic upside and makes ownership feel like real stakes — not just a profile picture.
- **[4·M]** Lore Codex: Holder-Authored Citizen Backstories — _💡 Forgotten Runes_
  - Steal Forgotten Runes' Book of Lore mechanic: give every awakened citizen a dedicated lore field on their agent page where only the holder (verified via wallet) can write a short backstory, origin myth, or mission statement. Lore is public on-chain (stored in a lightweight signed-message record or Supabase with wallet proof). The 10 most-upvoted lore entries per month get featured in the weekly Signal Report post to @4040hex and a spotlight on the homepage. This converts passive holders into worldbuilders and drives return traffic as they check engagement.
- **[4·M]** Sister-Collection Cross-Pollination Drop: 'The Crossing' — _💡 BAYC_
  - Steal BAYC's Mutant Serum airdrop logic — a one-time event where holders of TWO different FREELON CITY collections (e.g. own a FREELON + an OOGIE) become eligible to generate a 'Crossed Signal' artifact: a unique composite portrait blending the two characters' traits, auto-generated on-site, downloadable as a commemorative card. Not a new NFT mint — just a generated image with a signed proof of cross-ownership stored in Supabase. Drives holders of one collection to discover (and acquire) another, rewards multi-collection loyalty, and creates a natural sharing event ('look at my Crossed Signal').
- **[4·M]** Agent-Minted Artifact: Citizen Generates + Mints Its Own Output — _💡 Parallel Colony_
  - When an awakened citizen completes a notable output (long chat thread, generated image, or a completed quest), give the owner a one-tap 'MINT THIS ARTIFACT' button. The on-chain artifact is a lightweight ERC-721 containing a hash of the output + the citizen's token ID + block timestamp — proving the citizen DID this work at this moment. The owner pays gas (or we cover it via a thin relay). The artifact becomes a permanent proof-of-work trophy tied to that specific FREELON. Cost: one image-gen endpoint call + an ERC-721 deploy; no new backend logic. Position it as 'your citizen built this — now it's on-chain.'
- **[4·M]** Cross-Citizen Collaboration: Two Citizens Co-Author a Work — _💡 Virtuals Protocol_
  - Let an owner 'invite' a second FREELON (from the same or a different wallet) to collaborate on a shared output — the two agents produce a joint image, co-write a story beat, or debate each other. The resulting artifact is attributed to BOTH token IDs in metadata. This is a direct steal of Virtuals Protocol's ACP agent-to-agent commerce model stripped of tokenomics — two awakened agents transacting value (creative output) together. Surface this as 'CITIZEN × CITIZEN' — a share-worthy moment. Requires: a session handoff API between two /agent/[id] endpoints. No new economy mechanics needed.
- **[4·M]** Agent Commerce: Citizens Accept 'Commissions' from Other Holders — _💡 Virtuals Protocol_
  - Steal Virtuals Protocol's ACP request→escrow→deliver pattern stripped of token speculation. Build a lightweight 'Commission Board' on the city site: any holder can post a request ('I want a poem about my OOGIES #42' or 'generate a battle scene between Citizen 7 and Citizen 88'). Any holder whose AWAKENED citizen can fulfill it accepts and produces the output. HEX is spent by the requester (sink) and a portion lands in the fulfilling citizen's owner account. No ETH involved. This creates holder-to-holder activity, surfaces citizen capabilities, and produces shareable artifacts — three virality vectors at once.
- **[4·M]** Fetch.ai-Style Offline Task Queue: Assign Your Citizen a Task, Come Back to Results — _💡 Fetch.ai_
  - Steal Fetch.ai's autonomous offline-execution model. Let holders queue a task for their citizen before closing the app: 'Write a short story about your origin while I'm gone,' 'Generate 3 new portrait variations,' or 'Summarize your full memory.' The citizen completes the task asynchronously (standard LLM call), stores the result, and pushes a notification when done. The holder returns to find new content waiting. This breaks the 'must be present to get value' constraint — citizens feel alive even when their owner is offline. Requires: a simple task queue (Redis or Supabase queue) + push notification. No new AI infra.
- **[4·M]** Pudgy World Town Map — a visual ecosystem state shared publicly — _💡 Pudgy Penguins_
  - Steal Pudgy World's 12-towns map mechanic adapted for FREELON CITY: a public live map showing all 6 collection 'districts' (FREELONS, Crypt, OOGIES, Emile, SMILES, TCG) with real-time indicators — how many citizens active today, current streak leaders, recent work-history entries rolling ticker. No wallet needed to view. This is a distribution page: cold X visitors land here and see a living city. Currently the site has no page that communicates 'this place is alive right now.' The map is shareable ('look how many citizens are online') and doubles as a collection hub / world guide.
- **[4·M]** Merge the 5 'city is alive' surfaces into ONE: /live — _📁 phase3/freelon-city-site/app/live/page.tsx:22_
  - Direct hit on Billy's #1 problem (too much surface). We have FIVE overlapping proof-of-life pages reading nearly the same data: /live (transmissions+transforms+leaders), /transmissions (the same transmissions wall), /report (weekly winner+notables), /dashboard (numbers), /carrier-of-the-week (weekly featured citizen). The homepage already embeds CityWeekBand + CitizenMosaic + TransformsWall + ActivationProof, so the front door is covered. Collapse /transmissions + /report + /carrier-of-the-week INTO /live as tabs/sections (recent wall / this week / numbers), permanent-redirect the old URLs (same pattern as /start→/help, /archive→/collections). Keep the submit action and the Sunday Signal Report cron (it posts to /report?ref=sr- — repoint to /live#week). One clear 'the city is alive' destination beats five half-built ones competing for the same visit.
- **[4·M]** Weekly Signal Report → a designed, recurring SPECTATOR event people tune in for — _📁 phase3/freelon-city-site/app/report/page.tsx:11_
  - The Sunday 18:00 UTC Signal Report auto-posts already (cron + /api/og/report), but it reads as a stats dump. Reframe it as an appointment-viewing ritual with stakes and narrative: name the 'Civilization of the Week' as a storyline ('the SIGNAL-BORN pressed their claim — the ORACLES fell silent'), spotlight ONE citizen's week as a mini-story, and tease next week's contest. Give holders a reason to be present at post time (their citizen could be named). Recurring scheduled drama is the cheapest virality engine and the data pipeline is built. Copy-safe — it's about work/activity, not price.
- **[4·L]** Season Drops: Quarterly Citizen Trait Overlays — _💡 Moonbirds_
  - Run quarterly 'Seasons' themed around FREELON CITY lore events (Season 1: First Contact, Season 2: The Signal Deepens, etc.). Each Season releases 4,040 limited overlay NFTs — a cosmetic layer (animated frame, seasonal badge, holographic glitch effect) that can be applied to any awakened citizen's display card. Overlays are sink-only (cost HEX to apply, not mint). Holders who apply before a deadline get a 'Founder of Season N' permanent badge. Drives HEX consumption, creates urgency, and gives holders something to share each quarter. Adapted from battle-pass seasonal format but brand-premiumized.
- **[4·L]** Let the demo agent SHOW its render power, not just talk (proof inside the demo) — _📁 phase3/freelon-city-site/app/proof/page.tsx:5_
  - The owned product's most differentiated, most viral feature is image generation ('only your FREELON renders your character' — the whole /proof page). But the free demo is text-only, so a cold visitor never SEES the one thing that makes a FREELON different from ChatGPT. Give the demo ONE free, watermarked render of the flagship FREELON (heavily rate-limited, single use per session, demo-tier art only — never the owned money path), triggered by a starter chip like 'show me what you can make.' That single image is both the strongest sell AND an instant share asset. Bounded cost (1 render/session behind the existing budget guard); turns the abstract /proof argument into a thing they watched happen.

## ⚙️ TECH & PERF
_Cheaper, faster, more robust: homepage off force-dynamic (edge cache), next/image in NFT grids, kill the dead snipe path burning OpenSea quota, server-derived activation counts, mission-route test coverage._

- **[5·L]** ERC-6551 Token Bound Account: Citizen Wallet Holds Its Own Artifacts — _💡 Parallel Colony_
  - Steal Parallel Colony's ERC-6551 Token Bound Account implementation: each FREELON citizen NFT becomes its own wallet address. Any artifacts the citizen mints, any skill NFTs equipped, and any Crypt loot extracted are stored INSIDE the citizen's own wallet — so when the citizen NFT transfers, everything it owns moves with it. This dramatically increases the narrative value of a citizen ('this one has 14 artifacts and a Poet skill') and makes secondary sales more valuable. This is the single highest-leverage technical primitive for long-term collectibility. Requires: ERC-6551 registry deploy + TBA integration on the existing ERC-721 contract.
- **[4·S]** Flip homepage off force-dynamic — enable edge caching — _📁 phase3/freelon-city-site/app/page.tsx:52-57_
  - app/page.tsx has `export const dynamic = 'force-dynamic'` (line 57) with a comment citing IdentityGreeting/HeroMarketStat client islands — but those components were removed in the 2026-06-17 Algorithm review (confirmed removed at lines 103-104). The homepage is now a pure static RSC. Replace force-dynamic with `export const revalidate = 3600`. Vercel will serve it from CDN, cutting origin load on the highest-traffic route to near zero.
- **[4·S]** Short-circuit dead snipe bounty path burning OpenSea quota — _📁 phase3/freelon-city-site/lib/holder-tick.ts:178_
  - lib/holder-tick.ts:178 calls `creditSnipeBounties(address, tokens.tokenIds)` on every authenticated hex balance check. creditSnipeBounties (lib/economy-extras.ts:219) hits the OpenSea API for transfer events and then calls getRedSignal() per tokenId. The red-signal store is never populated — /api/market/red-signals was retired 2026-06-19 and always returns `{ signals: [] }`. Every holder tick wastes OpenSea quota + Redis reads on a code path that can never credit anyone. No-op creditSnipeBounties or remove the call.
- **[4·S]** Add a server-derived 'real activations this week' read so the count survives broken client analytics — _📁 phase3/freelon-city-site/app/api/play-event/route.ts:100_
  - Client events get blocked by ad-blockers, in-app webviews, and (as just found) dead components. The ground truth for the ONE number that proves revenue — paid awakenings — is already on the server: activate() writes a record (lib/missions/unlock-store.ts:88) and listActivatedTokenIds() enumerates them. Mirror the existing header-authed GET pattern from /api/play-event (route.ts:100-133) with a tiny GET /api/admin/activations returning {today, week, total, distinctWallets} computed from the unlock store. This is the un-fakeable denominator the Vercel funnel ratios get sanity-checked against. Decision: if Vercel activation_paid << server activation count, you know the client funnel is leaking instrumentation (not customers) and trust the server number for the business call.
- **[4·M]** Swap raw <img> tags in NFT grids to next/image for WebP + responsive srcset — _📁 phase3/freelon-city-site/components/CitizensBrowser.tsx:256_
  - CitizensBrowser.tsx:256 and MyCitizens.tsx:149 use raw `<img src={imageUrl(c.id)} loading='lazy' />`. Pinata/IPFS origins are already whitelisted in next.config for next/image — these bypasses all WebP conversion and responsive srcset generation. Same pattern in CityWeekBand.tsx:126, TopAgents.tsx:66, VaultClient.tsx:426, CollectionBrowser.tsx:188, and ArtefactGallery.tsx:52. The two highest-traffic grids (CitizensBrowser, MyCitizens) are the priority — these render 20-50 NFT images per page load.
- **[4·M]** Add integration test for mission route HEX debit/refund/day-claim path — _📁 phase3/freelon-city-site/app/api/citizens/[id]/mission/route.ts:1_
  - app/api/citizens/[id]/mission/route.ts is 583 lines containing HEX debit, free-run consumption, refund-on-failure, and daily claim logic — but has zero end-to-end test coverage. The vitest suite covers individual store helpers but not the combined POST handler. A single msw/vitest integration test covering: (a) debit success, (b) refund on LLM failure, (c) day-limit enforcement would catch regressions in the most financially sensitive route.

---
## How to sequence it
**NOW (this week — switch-flips + the leak):** retire the live snipe bounty (holder-tick.ts:178) + the market-coupled faucets · set real HEX mission prices (the sink) · `PAYMENTS_LIVE=true` ($12/$19 ETH rails) · register Feud + Versus missions · fix the my-citizens→/agent routing bug · share button + ?ref= on every owned render & agent output.

**NEXT (the bottleneck — distribution):** the recurring weekly spectacle (Guard-the-Pot live + a Freysa-style Signal Challenge) · 'Ask the City' daily free X character · Citizen Wrapped · autonomous agent X posts · per-surface OG cards + replay/share for Crypt · carry the demo memory into the owned agent · EVOLVE_LIVE.

**LATER (big bets / needs eng+legal):** fiat onramp + embedded social-login wallet (Privy/Pudgy — the mainstream unlock) · season-pass cadence · Mars Phase-1 read-only faction map + Hall of Records · play-YOUR-FREELONs Crypt deck + Crypt→HEX bridge · one premium color system · ERC-6551 token-bound citizen wallets (economy+legal review).

## ⛔ Drop / avoid
- **Any floor-support / anti-dump / price-coercion mechanic — in any form.** Several ideas drifted back toward it (listing bounty, sale-share faucet, 'snipe below floor'); the dump-deterrent stays dead. Never tie a HEX reward or penalty to a sell/list price.
- **New HEX faucets** before circulating-HEX telemetry exists — the economy is sink-only; the sink isn't even on yet (mission costs are 0).
- **On-chain referral *rewards* / token-bound accounts / agent-minted *mints*** — interesting, but each needs careful economy + legal review; park as 'later, gated'.

_Full 249 deduped ideas (incl. impact 1-3 long tail) available in the run output; this doc carries the 150 high-impact items._